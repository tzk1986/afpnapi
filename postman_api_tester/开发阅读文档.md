# 开发阅读文档（约束汇总）

版本基线：v1.1.5（报告中心能力增强版）
发布日期：2026-04-28
适用范围：postman_api_tester 模块、report_server.py、运行与报告链路

关联文档：

- `postman_api_tester/README.md`：用于确认当前交付目录、入口说明和新人使用路径。
- `postman_api_tester/操作手册.md`：用于确认当前操作流程、报告中心行为和使用侧说明。
- `postman_api_tester/快速命令参考.md`：用于确认命令入口、参数顺序、优先级文案是否同步。
- `postman_api_tester/最终交付清单.md`：用于确认对外交付范围、验收要求和发布口径是否同步。

## 1. 开发前必读条件（新增）

开始任何开发前，必须先完整阅读以下文件并确认当前改动是否触及其中约束：

1. postman_api_tester/操作手册.md
2. postman_api_tester/快速命令参考.md
3. postman_api_tester/config.py
4. report_server.py
5. postman_api_tester/postman_api_tester.py

未完成上述阅读，不允许直接修改核心逻辑（token处理、报告生成、报告服务接口、分页与筛选行为）。

## 2. 开发流程约束（新增）

**方案讨论与实施分离约束**：
- 新功能开发分为"方案讨论"与"实施"两个阶段
- 在方案讨论阶段，禁止对任何代码文件进行修改（包括 `report_server.py`、`templates/*.html`、`postman_api_tester/*.py` 等）
- 只有收到用户明确的口令 **"开始实施"** 后，才允许进入代码修改、测试与提交阶段
- 方案讨论期间仅允许：读取文件、分析代码结构、输出方案文档、更新约束文档
- 方案讨论后，新增的方案文档名称增加日期，格式为 `YYYY-MM-DD-方案名称.md`，并存储在 `postman_api_tester/方案讨论` 目录下
- 方案讨论后最终实施前需要确认，方案实施涉及的业务范围，修改的页面有哪些，涉及到原有的哪些功能，需要做确认
- 方案实施前，需要把本次讨论生成的所有方案进行整理合并到一起，统一实施，不应该有实施遗漏
- 方案实施后，用于验证的文件，如果可以复用，进行保留，并存储在 `test_data` 目录下，并在操作手册中记录，验证文件如何使用，以便后续复用

## 3. 当前有效约束清单

### 2.1 运行与环境约束

1. Python 版本要求：3.10+
2. 依赖安装入口：requirements.txt
3. 建议在项目根目录执行命令：d:/tangzk/py/seldom-api-testing
4. **TOKEN 支持环境变量注入**：设置 `POSTMAN_TOKEN` 环境变量可覆盖 config.py 的默认 TOKEN；未设置则为空字符串（触发自动登录）
5. **BASE_URL 支持环境变量注入**：设置 `POSTMAN_BASE_URL` 环境变量可覆盖 config.py 的默认 BASE_URL
6. 报告服务端口/主机支持环境变量：`REPORT_SERVER_PORT`（默认 5000）、`REPORT_SERVER_HOST`（默认 0.0.0.0）
7. **生产建议**：安装 `waitress`（`pip install waitress`）后，report_server.py 自动切换为 waitress WSGI 服务器；未安装则降级使用 Flask 开发服务器

### 2.2 测试执行参数约束

1. CLI 参数顺序：
   python -m postman_api_tester.postman_api_tester <postman_file> [base_url] [output_dir] [token] [results_per_page]
2. token 优先级：命令行/代码入参 token > 环境变量 `POSTMAN_TOKEN` > postman_api_tester/config.py 的 TOKEN > 自动登录获取
3. base_url 优先级：命令行/代码入参 base_url > 环境变量 `POSTMAN_BASE_URL` > postman_api_tester/config.py 的 BASE_URL > 从 Postman 集合解析
4. output_dir 优先级：命令行/代码入参 output_dir > postman_api_tester/config.py 的 REPORT_OUTPUT_DIR > 项目根目录 reports/
5. 报告分页参数 results_per_page 默认 30（报告页建议控制在 1~100）
6. **请求超时（集中配置）**：超时参数现由 `postman_api_tester/config.py` 统一提供（`REQUEST_CONNECT_TIMEOUT=10`、`REQUEST_READ_TIMEOUT=30`），可通过同名环境变量覆盖。`execute_test()` 运行时从 config 动态读取，禁止在执行层硬编码数字
7. **token 并发安全**：`_auth_token` 现为实例变量，不同并发任务互相隔离，禁止恢复类变量方案
8. **details.json 安全**：写入详情文件时，请求头中 `authorization`、`token`、`x-token`、`x-access-token`、`access-token` 字段值统一替换为 `***`，不可明文存储
9. **URL 解析兼容约束（导入执行一致性）**：执行层解析 `request.url` 时，若 `path` 缺失必须回退读取 `raw`；当 `raw` 为绝对 URL 或 `{{baseUrl}}/...` 模式时均应可执行，禁止因仅有 `raw` 结构而在执行阶段静默丢失接口

### 2.3 报告产物约束

每次执行必须生成同名前缀的三类文件：

1. postman_report_时间戳.html（主报告）
2. postman_report_时间戳_details.json（详情数据）
3. postman_report_时间戳_meta.json（历史索引/对比元数据）

### 2.4 报告中心约束（report_server.py）

1. 服务默认监听 0.0.0.0:5000
2. 报告目录读取优先级：
   POSTMAN_REPORTS_DIR 环境变量 > REPORTS_DIR 环境变量 > config.py 中 REPORT_OUTPUT_DIR > 项目根目录 reports/
3. 单报告数据页分页规则：
   默认 20 条/页；上限 100 条/页
4. 支持报告历史对比：新增接口、移除接口、状态变化、成功率变化
5. 并发写回同一报告时使用按报告名粒度的写锁，避免并发覆盖
6. **上传文件名安全**：`original_name` 在写入任务状态前已清洗（仅保留字母/数字/汉字/`-._()（）【】`），防止路径穿越与注入攻击
7. **任务内存上限（集中配置）**：`RUN_JOBS` 上限由 `config.py` 的 `RUN_JOBS_MAX`（默认 200）控制，可通过同名环境变量覆盖；不可移除清理逻辑
8. **报告列表缓存**：`list_reports()` 使用 30 秒 TTL 缓存，新报告生成时通过 `_invalidate_reports_cache()` 主动清除；不可在缓存有效期内绕过
9. **base_url SSRF 防护**：`run_postman_tests()` 和 `/api/run-postman` 接口均校验 base_url 仅允许 `http`/`https` 协议；禁止删除此校验
10. **结构化日志**：postman_api_tester.py 与 report_server.py 均使用 `logging` 模块；禁止在核心路径新增 `print()` 替代 `logger.*`
11. **健康检查端点**：`/health` 端点返回 `{"status": "ok", "timestamp": "..."}` 用于监控；不可删除
12. **生产服务器**：优先使用 waitress；禁止在生产环境将 Flask `debug=True`
13. **HTML 模板外置（固定）**：报告中心页面渲染统一使用 `templates/` 目录下的外置模板（Jinja2 `render_template`）。不再支持 `inline` 模式与内嵌模板回退分支，模板缺失应直接按异常暴露并修复源模板
14. **报告删除能力**：报告中心支持删除单份历史报告，并同时清理其主报告 HTML、`_details.json`、`_meta.json` 以及 `_page_*.html` 分页子页。删除逻辑必须限定在 `REPORTS_DIR` 目录内，禁止删除目录外文件
15. **首页轻量化约束**：报告首页仅加载列表摘要字段（报告名、集合名、来源、生成时间、汇总等），禁止再将完整 `results` / `request_info` / `response_info` 批量注入首页模板；详情数据必须继续通过详情接口或详情文件按需读取
16. **数据库迁移反馈约束**：执行层在 `FAILED`/`ERROR` 结果中应输出 `db_feedback` 结构化字段，并基于常见连接异常、认证异常、SQL兼容异常、对象缺失、字符集及类型兼容异常给出建议；禁止删除该字段或将其改为仅日志可见
17. **可选接口执行约束**：上传执行支持“全量执行 / 仅执行已选接口”两种模式。相关参数与边界值需统一由 `postman_api_tester/config.py` 管理（`ENABLE_SELECTIVE_RUN`、`COLLECTION_PREVIEW_MAX_ITEMS`、`RUN_STATUS_POLL_INTERVAL_MS`、`RUN_RESULTS_PER_PAGE_*`），禁止在页面脚本或服务端路由中继续硬编码默认值
18. **导出范围约束**：报告导出支持 `full`（全量）与 `report_only`（仅本次报告接口）两种范围，默认范围与开关由 `config.py` 统一管理（`REPORT_EXPORT_DEFAULT_SCOPE`、`REPORT_EXPORT_ALLOW_REPORT_ONLY`、`REPORT_EXPORT_INCLUDE_AUTH_DEFAULT`）。在禁止 `report_only` 时必须自动降级 `full`，禁止返回 500
19. **文档闭环约束**：每次功能收尾或发布前，`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 四份文档必须同步到同一版本号与发布日期，并同步新增能力说明（执行范围、导出范围、配置项）；禁止仅更新单份文档
20. **外置模板约束**：所有前端 UI 改动必须在 `templates/` 目录下的 `.html` 文件中进行。`report_server.py` 已移除内嵌模板（`INDEX_TEMPLATE`、`REPORT_VIEW_TEMPLATE`）与 `render_with_fallback()` 兜底分支；新增功能、样式调整、交互优化统一在外置模板实现
21. **人工用例能力约束**：报告页支持人工新增/编辑/删除用例，数据写入 `_meta.json` 的 `manual_cases` 字段；默认目录由 `config.py` 的 `MANUAL_CASE_FOLDER_NAME` 提供，页面允许手工覆盖填写
22. **排除显示约束**：报告结果支持对单条用例打排除标记并置灰展示；页面必须提供“显示/隐藏已排除”开关，隐藏时不展示已排除项，显示时保留置灰态
23. **统一导出约束**：导出 JSON 必须为单一文件并统一合并：原始集合（按最新请求参数回填） + `manual_cases` - `manual_exclusions`；禁止拆分为多个导出文件
24. **路由-函数签名一致性约束**：凡是路由层以关键字参数调用内部函数（如 `include_excluded=...`），必须同步校验被调用函数签名是否接收该参数；新增筛选参数时，必须同时更新函数签名、返回字段与模板消费字段，禁止只改路由不改函数
25. **人工用例可维护性约束**：`manual_cases` 条目必须具备稳定 `id`（新增时生成，更新/删除时按 `id` 定位），禁止依赖 `name/url` 等可变字段做唯一定位
26. **运行时冒烟回归约束（必做）**：涉及 `report_server.py` 路由、筛选、导出、人工用例改动后，提交前至少验证：`/health`、`/`、`/report-view`、`/api/report-results?include_excluded=true/false`、`/api/manual-cases` 的 add/update/delete、`/api/export-collection`；禁止仅以静态检查代替运行验证
27. **最小闭环排查法约束（复用）**：发现问题时按“日志定位 -> 最小复现 -> 双态参数验证（true/false）-> CRUD 闭环验证 -> 数据回滚清理”执行，并在约束更新记录中沉淀根因与验证命令，便于后续复用
28. **导出参数完整性约束（新增）**：导出集合（尤其人工用例）必须保留完整请求参数，至少包含 `method`、`url`、`headers`、`params`、`body`；对多 Body 模式还必须保留 `body_mode/body_data` 语义，禁止导出仅保留 method/url 导致参数丢失
29. **导入可解析性约束（新增）**：导入链路必须可解析导出文件中的全部请求数据结构（含 `request.url.raw/path/query`、headers、params、body 各模式），并保证“可预览的数据应可执行”，禁止出现导入后请求体为空或执行行为退化
30. **ad-hoc 直接执行约束（新增）**：报告中心首页支持通过“新增接口测试”按钮进入独立页面并执行生成报告。相关开关与边界值必须由 `postman_api_tester/config.py` 统一管理（`ENABLE_ADHOC_RUN`、`ADHOC_MAX_ITEMS`、`ADHOC_DEFAULT_COLLECTION_NAME`），服务端必须继续复用现有任务队列与 `run_postman_tests` 执行链路，并保持 `base_url` 与请求 URL 的 `http/https` 协议校验
31. **ad-hoc 路由与入队约束（新增）**：`/api/run-ad-hoc-tests` 仅接收 JSON 请求；`cases` 必须是非空数组且数量不得超过 `ADHOC_MAX_ITEMS`。后端需先将 ad-hoc 输入规范化为 Postman Collection v2.1 JSON，并写入 `uploaded_collections/<job_id>.json`，再复用现有任务状态接口与报告生成链路；禁止为 ad-hoc 另起一套独立执行/轮询协议
32. **ad-hoc URL 与 Body 语义约束（新增）**：ad-hoc 请求 URL 仅允许三种合法形态：绝对 `http/https`、`{{baseUrl}}/...` / `{{base_url}}/...` 变量地址、或配合 `base_url` 的相对路径。Body 模式必须与既有请求构建逻辑保持一致，统一支持 `none/raw/urlencoded/formdata/graphql/binary`，禁止 ad-hoc 与导入/重试链路出现语义漂移
33. **前端设计一致性约束（新增）**：新增或改造 `templates/` 下的编辑器、表单、按钮和分组样式时，必须优先复用已有页面（如 `templates/report_view.html` 中的编辑重试、人工用例编辑器）已采用的设计语言与交互结构。若确需引入新的设计风格，必须同步评估并更新同类旧页面，禁止 ad-hoc、编辑重试、人工用例等相邻能力长期存在并列但风格割裂的界面

### 2.5 局域网访问约束

1. 报告中心可通过 http://127.0.0.1:5000 本机访问
2. 同局域网访问需要放通 5000 端口
3. 推荐执行脚本：allow_report_server_firewall.ps1

## 3. 每次修改后更新本文件的命令（新增）

在项目根目录执行以下 PowerShell 命令，将“本次改动中疑似约束变更”的候选行自动追加到本文件末尾，作为待确认记录：

```powershell
$doc = "postman_api_tester/开发阅读文档.md"
$ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
$pattern = "优先级|默认|必须|限制|上限|下限|分页|token|base_url|report_output_dir|POSTMAN_REPORTS_DIR|REPORTS_DIR|5000|status|meta|details"
$files = @(
  "postman_api_tester/config.py",
  "postman_api_tester/postman_api_tester.py",
  "report_server.py",
  "postman_api_tester/README.md",
  "postman_api_tester/操作手册.md",
  "postman_api_tester/快速命令参考.md",
  "postman_api_tester/最终交付清单.md"
)
"`n## 约束更新记录（$ts）`n" | Add-Content $doc
rg -n -i $pattern $files | Add-Content $doc
"`n" | Add-Content $doc
```

执行后请人工二次整理：

1. 删除误匹配行
2. 将真实变更写入“当前有效约束清单”对应章节
3. 保持优先级、默认值、边界值三类信息准确

## 4. 建议的开发收尾检查

每次提交前至少确认：

1. README/操作手册/快速命令参考/最终交付清单 与代码行为一致
2. 报告目录优先级、token优先级未被破坏
3. 报告三件套（html/details/meta）仍可完整生成
4. 本文档已更新到最新约束状态
5. 四份操作文档版本号与发布日期已一致，并包含本次能力变更说明

## 约束更新记录（2026-04-23 09:37:54）

- 文档闭环收口：README、操作手册、快速命令参考、最终交付清单统一更新为 v1.0.9 / 2026-04-23。
- 统一新增能力说明：接口选择执行（全量 / 仅已选）与导出范围（full / report_only）。
- 统一配置口径：新增配置项由 `postman_api_tester/config.py` 集中管理，避免页面和路由硬编码。
- 本文件同步更新：版本基线升级至 v1.0.9，并新增“文档闭环约束”与收尾检查项。
## 约束更新记录（2026-04-23 10:45:33）

- 新增外置模板约束（约束20）：外置模板为UI改动的唯一入口，内嵌模板不再修改。
- 已选接口列表功能：在 `templates/index.html` 中添加"已选接口实时列表"面板，支持：
  - 双列并置展示（可选接口 vs 已选接口）
  - 实时同步：选中/取消勾选即时更新已选列表
  - 联动过滤：筛选条件对两列表同时生效，已选项保留
  - 快速操作：右列可单项移除（×按钮），无需回到左列
  - 实时计数：显示"已解析 N 个，已选 M 个"
  - 移动端适配：小屏自动堆叠为单列
- 代码实现：
  - 添加 CSS 类：.selector-container / .selected-apis-list / .selected-api-item 等
  - 新增 JavaScript Map：selectedApis，用于维护选中状态
  - 新增函数：updateSelectedList / removeSelectedApi / clearSelectedApis / syncMultiSelectState / syncSelectedApis
  - 修改函数：renderPreviewApis / selectAllPreviewApis / clearPreviewApiSelection / selectedPreviewPaths
  - 后端 API 无改动（使用现有 /api/collection-preview 和 /api/run-postman）

## 约束更新记录（2026-04-23 11:47:21）

- 选择区微调：新增筛选命中提示（显示“筛选: 当前命中/总量”），提升筛选状态可见性。
- 交互微调：筛选输入框支持 `Esc` 一键清空并恢复全量可选接口视图。
- 批量操作保护：`反选` 与 `清空筛选结果` 增加确认弹窗，避免误操作。
- 反馈增强：批量操作执行后在状态区输出影响接口数量，便于用户确认结果。
- 本轮改动范围：仅外置模板 `templates/index.html`，遵循外置模板约束，不修改内嵌模板。

## 约束更新记录（2026-04-24 16:20:00）

- 版本基线升级：`v1.1.0`（人工用例增强与统一导出版），发布日期同步为 2026-04-24。
- 新增人工用例能力：支持在报告页保存人工用例（默认目录可配置且允许手填）、编辑与删除。
- 新增排除可视化能力：排除项置灰，并提供“显示/隐藏已排除”开关。
- 导出统一收口：导出 JSON 统一为单文件，按“原始回填 + 人工新增 - 已排除”合成。

## 约束更新记录（2026-04-24 18:30:00）

- 模板渲染收口：`report_server.py` 已删除内嵌模板与 `inline/external` 回退分支，首页与报告页统一使用 `render_template` 渲染外置模板。
- 配置收口：`postman_api_tester/config.py` 移除 `REPORT_TEMPLATE_MODE` 配置项，避免无效配置造成误解。

## 约束更新记录（2026-04-24 19:20:00）

- 本次运行发现问题1：`/api/report-results` 在 `include_excluded` 参数路径出现 500，根因是路由调用参数与 `filter_report_results()` 函数签名不一致。
- 本次运行发现问题2：人工用例新增后缺少稳定 `id`，导致 update/delete 链路不可稳定闭环。
- 本次复用方法沉淀：采用“日志定位 -> 最小复现 -> 双态参数验证（`include_excluded=true/false`）-> CRUD 闭环验证（add/update/delete）-> 导出验证 -> 回滚清理”流程，可快速暴露静态检查无法发现的运行时问题。
- 清理动作要求：运行验证生成的临时导出文件需在收尾阶段清理，避免污染后续评估与交付目录。

## 约束更新记录（2026-04-24 20:30:00）

- **新增后端路由 `/api/proxy-request`**（`report_server.py`）：接收 `{method, url, headers, params, body}`，代理发起真实 HTTP 请求，返回 `{status_code, elapsed_ms, response_headers, response_body}`。SSRF 防护：仅允许 `http/https` 协议，超时从 `postman_api_tester/config.py` 的 `REQUEST_CONNECT_TIMEOUT` / `REQUEST_READ_TIMEOUT` 动态读取，禁止硬编码。
- **人工用例编辑器重构为三段式流程**（`templates/report_view.html`）：
  - 段一：填写请求信息（名称/目录/方法/URL/Headers/Params/Body）→ 点击「发送请求」
  - 段二：展示真实 HTTP 响应（状态码徽章、耗时、响应头可折叠、响应体）→ 自动预选 PASSED/FAILED
  - 段三：确认结果（PASSED/FAILED 单选）→ 点击「保存」写入 `manual_cases`
  - 编辑模式额外提供「直接保存（不重发）」按钮，保留原有响应数据
  - 旧版（含响应信息手填字段）已包裹在 `/* 旧版实现 */` 注释块中完整保留
- **列表行内联排除按钮**（`templates/report_view.html`）：`renderResults()` 每行操作列直接显示「排除」/「取消排除」按钮，无需进入详情对话框。依赖 `/api/report-results` 返回的 `exclusion_key` 字段（该字段后端已存在，line 659），刷新时调用 `reloadResults(currentResultsPage)`。
- **人工用例列表状态列升级**：`renderManualCases()` 状态列由"已排除徽章"改为显示真实 PASSED/FAILED 徽章 + 排除状态，样式复用 `.status-passed` / `.status-failed` 类。
- **新增模块级变量**：`_mcLastResponse`（暂存最近一次代理响应）、`_mcEditingCase`（暂存当前编辑用例），编辑器关闭时自动清空。
- **新增 JS 函数**：`sendManualRequest()`、`confirmSaveManualCase(caseId)`、`directSaveManualCase(caseId)`、`toggleResultExclusion(encodedKey, excluded)`。
- **Python 语法验证**：`ast.parse` 通过，无语法错误。

## 约束更新记录（2026-04-24 21:10:00）

- 文档版本闭环：`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 统一升级为 v1.1.1，发布日期保持 2026-04-24。
- UI 一致性优化收口（`templates/report_view.html`）：
  - 结果列表与人工用例列表操作按钮统一为紧凑风格，避免样式不一致。
  - 操作列采用不换行布局，规避“取消排除”按钮换行导致的表格抖动。
  - 人工用例列表字段与报告结果视图对齐，补齐 `status_code`、`message`、`err_code`。
  - 人工用例新增“详情查看与编辑重发”能力，复用统一详情弹层与既有排除链路。
- 运行验证收口：接口冒烟验证以动态报告名（来自 `/api/reports` 返回 `report_name`）驱动，避免因手工截断报告名导致 404 误判。

## 约束更新记录（2026-04-24 22:30:00）

- 文档闭环同步：`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 统一升级为 v1.1.2，发布日期保持 2026-04-24。
- 报告页可观察性增强：新增“页面模板更新时间”小标记，便于排查缓存导致的旧页面问题。
- 人工用例详情一致性增强：详情弹层中的响应体展示与报告详情统一（预览/展开）。
- 人工用例可维护性增强：编辑时回显历史请求信息（Headers/Params/Body），支持改参后重新发送。
- 后端数据完整性修复：`_normalize_manual_case` 保留 `request_info/response_info` 与 `elapsed_ms`，并兼容旧字段回填，避免详情与编辑回显数据丢失。

## 约束更新记录（2026-04-24 23:05:00）

- 文档闭环同步：`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 统一升级为 v1.1.3，发布日期保持 2026-04-24。
- 导出计数一致性修复：`/api/export-collection` 返回字段对齐（`manual_case_count`、`manual_case_exported_count`），导出弹窗中的人工用例数量与实际导出一致。
- 排除过滤一致性修复：导出时排除匹配按报告结果的 `folder` 规则执行，避免因目录链路格式差异导致已排除接口仍被导出。
- 复现与验证样例：以 `测试数据01.html` 验证，修复后导出结果中被排除的 `人工提现查询` 不再出现，且人工用例统计正确。

## 约束更新记录（2026-04-27 13:55:00）

- 版本基线升级：`v1.1.4`（安全与稳定性修复版），发布日期同步为 2026-04-27。
- 编辑界面切换优化：报告页手工编辑区（mc）与重试编辑区（er）切换逻辑隔离，避免 Body 模式按钮互相污染。
- 报告页修复：`templates/report_view.html` 中 `switchManualBodyMode` 改为仅在 `#mc-body-editor` 容器内切换，避免污染 er 编辑器状态。
- Body 类型选择优化：统一支持 `none/raw/urlencoded/formdata/graphql/binary`，并保持导出、重试、代理请求构建逻辑一致。
- 重试安全修复：`/re-request-api` 新增 `http/https` 协议与 netloc 校验，和 `/api/proxy-request` 对齐。
- 脱敏修复：`_strip_auth_headers` 补齐 `x-token`、`x-access-token`、`access-token`。
- 请求体能力补齐：`_set_request_body` 增强为支持 `none/raw/urlencoded/formdata/graphql/binary`，并兼容 legacy 数据。
- 路由复用优化：提取 `_build_request_kwargs(...)`，统一 `/re-request-api` 与 `/api/proxy-request` 请求体构建逻辑。
- 清理重复定义：删除重复 `import time as _time` 与重复 `_REPORTS_CACHE/_REPORTS_CACHE_TTL` 声明。
- 可复用验证脚本：新增 `test_data/smoke_report_server_20260427.py`，覆盖 `/health`、`/`、`/report-view`、`/api/report-results true/false`、`/api/manual-cases` CRUD、`/api/export-collection`、非法 scheme 拦截验证。

## 约束更新记录（2026-04-27 14:12:00）

- 本次问题现象：人工用例导出后二次导入，预览区解析出 2 条接口，但上传执行仅跑 1 条。
- 根因定位：执行层 URL 解析曾仅依赖 `request.url.path`，对仅含 `request.url.raw` 的接口未覆盖，导致执行阶段被静默跳过。
- 修复策略：`_build_url_from_dict` 增加 `raw` 回退解析，兼容绝对 URL 与 `{{baseUrl}}/...` 模式。
- 验证结果：同一导入文件复测后，执行总数从 1 恢复为 2（失败原因为未登录，不再是未执行）。

## 约束更新记录（2026-04-27 14:35:00）

- 本次问题现象：人工用例在原始报告中可通过，但导出后二次导入执行失败，详情中请求体显示“无”。
- 根因定位：导出链路追加人工用例时仅写入 method/url，未写入 headers/params/body，导致导入后请求参数缺失。
- 修复策略：导出人工用例时复用统一请求构建逻辑，同步写入 URL、Headers、Body（含 body_mode/body_data 语义）。
- 新增约束：导出参数完整性约束 + 导入可解析性约束，要求导出/导入链路保持请求语义不丢失。
- 验证结果：复测导出文件中人工用例已保留完整请求参数，导入解析后请求体恢复正常。

## 约束更新记录（2026-04-28 09:10:00）

- 版本基线升级：`v1.1.5`（报告中心能力增强版），发布日期同步为 2026-04-28。
- 新增 ad-hoc 能力：报告中心首页支持通过“新增接口测试”按钮进入独立页面并执行生成报告，无需先上传完整 Collection 文件。
- 配置收口：新增 `ENABLE_ADHOC_RUN`、`ADHOC_MAX_ITEMS`、`ADHOC_DEFAULT_COLLECTION_NAME`，统一由 `postman_api_tester/config.py` 管理。
- 安全与兼容要求：ad-hoc 路由继续复用任务队列和执行链路，保持 `base_url`/URL 的 `http/https` 校验，报告三件套与历史对比/导出/人工用例能力保持兼容。
- 文档闭环同步：`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 已统一升级为 v1.1.5 / 2026-04-28。

## 约束更新记录（2026-04-28 15:30:00）

- 文档闭环再校准：`README.md`、`操作手册.md`、`快速命令参考.md`、`最终交付清单.md` 继续保持 `v1.1.5 / 2026-04-28` 一致口径，并补齐 ad-hoc 能力说明、配置项、交付验收描述。
- ad-hoc 约束细化：新增 `/api/run-ad-hoc-tests` 路由入参、临时 Collection 落盘、复用既有任务轮询链路等约束，避免文档只描述能力不描述边界。
- 使用侧口径补齐：四份操作文档均补充 ad-hoc 的配置项（`ENABLE_ADHOC_RUN`、`ADHOC_MAX_ITEMS`、`ADHOC_DEFAULT_COLLECTION_NAME`）、URL 规则和标准报告产物说明。

## 约束更新记录（2026-04-28 16:05:00）

- 首页轻量化收口：报告中心首页的 ad-hoc 大表单已下沉到独立页面，首页仅保留“新增接口测试”入口按钮，减少首页复杂度。
- 新增页面路由：`/adhoc-run` 作为 ad-hoc 独立页面入口，继续复用既有 `/api/run-ad-hoc-tests` 入队与状态轮询链路。
- 文档口径同步：四份操作文档已统一改为“首页按钮进入独立新增接口测试页”描述，避免继续沿用“首页直接录入”旧表述。

## 约束更新记录（2026-04-28 18:30:00）

- ad-hoc 页面样式收口：`templates/adhoc_run.html` 的表单模式已改为复用 `templates/report_view.html` 同类的紧凑 KV 行式编辑器结构，避免自建表格样式导致输入框与删除按钮挤压。
- 设计一致性约束新增：新增“前端设计一致性约束”，要求后续新增页面优先沿用既有编辑器/表单风格；若引入新设计，必须同步更新旧页面，避免界面割裂。

## 约束更新记录（2026-04-28 18:45:00）

- 报告页编辑链路视觉统一（第二轮）：`templates/report_view.html` 中“编辑重试”“新增/编辑人工用例”“保存为人工用例（由 prompt 升级为同页编辑面板）”已统一为同一套编辑器布局（顶部字段栅格 + 请求参数分组 + JSON/表单切换 + 双列 Headers/Params + Body 独占行）。
- 人工用例详情页头部信息细化：统一显示“配置 URL/实际请求 URL”、状态徽标与“人工用例”标签，和报告详情信息结构保持一致。
- 保持功能不变：保留原有接口、字段 ID、按钮动作与保存链路，仅做模板结构与样式收口。

## 约束更新记录（2026-04-29 09:50:00）

- 报告详情 JSON 展示升级：`templates/report_view.html` 详情弹层中的“请求头/查询参数/请求体/响应头/响应体”统一改为可折叠树形 JSON Viewer，支持按块“展开/收起/复制”，提升大对象可读性并降低长 JSON 滚屏成本。
- 响应体大数据兼容保持：继续复用预览/完整两档渲染策略与安全截断上限（防止超大响应阻塞页面），仅替换展示形态为可展开树视图。
- 兼容性要求：不改变现有详情弹层按钮行为（编辑重试、保存为人工用例、排除/取消排除、关闭）与后端接口契约。

## 约束更新记录（2026-04-29 09:58:00）

- 详情 JSON 交互细化（Chrome Preview 风格）：树形节点由“键值行 + 箭头”组成，支持对象/数组多层嵌套逐层独立展开与收起（例如 `response.body.data.list` 可单独展开，不影响同级节点）。
- 可读性细节：节点摘要显示关键字段预览（如 `data: {list: […], pageNum: 1, ...}`），并保留每个分区“展开/收起/复制”工具按钮，兼顾快速浏览与深层排查。

## 约束更新记录（2026-04-29 10:05:00）

- 详情 JSON 视觉再优化：JSON Viewer 区域调整为严格左对齐与编辑器风格呈现（浅色代码底、行级 hover、高亮缩进线、水平滚动承载长行），避免出现“内容视觉居中”观感。
- 展示目标统一：详情弹层内 JSON 数据展示应优先遵循“代码编辑器阅读体验”（左对齐、层级清晰、长行不挤压、可逐层折叠）。

## 约束更新记录（2026-04-29 10:08:00）

- 人工用例详情页展示统一：`openManualCaseDetail`/`renderManualCaseDetailPanels` 已切换为与普通报告详情相同的 JSON Viewer 卡片模板（请求头/查询参数/请求体/响应头/响应体），统一支持“展开/收起/复制”。
- 渲染链路统一：人工用例详情不再走旧 `pre` 文本渲染，改为复用 `renderDetailPanels` + `renderResponseBody`，确保样式、交互、截断策略一致。

## 约束更新记录（2026-04-29 11:02:00）

- 人工用例编辑默认模式调整：`templates/report_view.html` 的人工用例编辑器进入时默认启用“表单编辑”，并将模式按钮顺序统一为“表单编辑 → JSON 编辑”。
- 编辑体验一致性收口：人工用例编辑器的模式切换器样式复用“新增接口测试”页同款视觉（同类间距、边框、按钮激活态），确保两处操作体验一致。

## 约束更新记录（2026-04-29 11:12:00）

- 新增接口界面一致性收口：`templates/adhoc_run.html` 的“新增接口”编辑区已对齐 `templates/report_view.html` 人工用例编辑器结构（请求参数面板、编辑方式切换、KV 行编辑、Postman 风格 Body 模式分区）。
- 风格统一约束细化：新增接口测试页与人工用例新增页需保持同一视觉与交互语言，并持续与报告中心主视觉一致；后续同类编辑区改造优先复用现有样式类与组件结构。

## 约束更新记录（2026-04-29 11:20:00）

- 按钮样式一致性补齐：`templates/report_view.html` 的人工用例区按钮（新增/刷新、发送、取消、直接保存、保存）统一到新增接口测试页同款颜色与尺寸规范（主按钮蓝底白字、次按钮白底蓝字、统一圆角与内边距）。
- 兼容性要求：本次仅调整样式类，不修改按钮事件与数据处理逻辑，保证人工用例新增/编辑/发送/保存功能不受影响。

## 约束更新记录（2026-04-29 11:28:00）

- 人工用例编辑区按钮再对齐：`#manualEditor` 范围内的“表单编辑/JSON编辑”、“Body 类型按钮”、“+添加”按钮已按新增接口测试页当前按钮体系统一（默认蓝底白字、激活态深色、统一 10x12 内边距与 10px 圆角）。
- 作用域约束：样式调整限定在 `#manualEditor`，不影响编辑重试区与其他页面逻辑/事件。

## 约束更新记录（2026-04-29 11:38:00）

- 列表可读性增强：报告中心列表（`templates/index.html`）与数据视图中的人工用例管理列表（`templates/report_view.html`）在长文本场景下统一采用“单行省略号 + hover 浮层显示完整内容（title）”。
- 兼容性要求：仅做展示层增强，不改动列表数据结构、筛选、跳转与人工用例相关业务逻辑。

## 约束更新记录（2026-04-29 11:52:00）

- Tooltip 组件统一升级：报告中心与数据视图统一启用自定义 tooltip 组件（`data-tooltip` + 事件代理），支持多行换行展示与文本可选复制，不再依赖浏览器原生 `title`。
- 交互约束：tooltip 需支持从目标元素移动到浮层继续停留（便于复制），并在滚动/缩放时自动重定位，避免遮挡与闪烁。
- 兼容性要求：仅替换展示层提示机制，不改变表格省略规则、列表点击行为与人工用例操作链路。