# 开发阅读文档（约束汇总）

版本基线：v1.33.29（报告界面优化）
发布日期：2026-08-19
适用范围：postman_api_tester 模块、报告服务（`python -m postman_api_tester` 启动）、运行与报告链路

关联文档：

- `postman_api_tester/README.md`：用于确认交付目录、入口说明和新人使用路径。
- `postman_api_tester/操作手册.md`：用于确认当前操作流程、报告中心行为和使用侧说明。
- `postman_api_tester/快速命令参考.md`：用于确认命令入口、参数顺序、优先级文案是否同步。
- `postman_api_tester/最终交付清单.md`：用于确认对外交付范围、验收要求和发布口径是否同步。

---

## 1. 开发前必读条件

开始任何开发前，必须先完整阅读以下文件并确认当前改动是否触及其中约束：

1. `postman_api_tester/操作手册.md`
2. `postman_api_tester/快速命令参考.md`
3. `postman_api_tester/config.py`
4. `postman_api_tester/report_server.py`
5. `postman_api_tester/postman_api_tester.py`

未完成上述阅读，不允许直接修改核心逻辑（token 处理、报告生成、报告服务接口、分页与筛选行为）。

## 2. 开发流程约束

方案讨论与实施分离：

1. 新功能开发分为”方案讨论”和”实施”两个阶段。
2. 方案讨论阶段禁止修改代码文件（包括 `postman_api_tester/report_server.py`、`templates/*.html`、`postman_api_tester/*.py`）。
3. 只有收到用户明确口令”开始实施”后，才允许进入代码修改、测试与提交阶段。
4. 方案讨论期间仅允许：读取文件、分析代码结构、输出方案文档、更新约束文档。
5. 新增方案文档命名格式：`YYYY-MM-DD-方案名称.md`，放在 `postman_api_tester/方案讨论`。
6. 实施前必须确认：业务范围、涉及页面、对原功能的影响。
7. 实施前需对本次讨论方案进行合并梳理，避免遗漏。
8. 实施后可复用的验证文件保留到 `test_data`，并在操作手册记录复用方式。

### 2.1 AI 定时自动升级豁免条件

由 `.claude/scheduled_tasks.json` 中的 cron 任务触发的自动研发升级（每天 20:30），在满足以下全部前提时可跳过约束 2.3（”开始实施”人工确认）：

1. **分支隔离**：必须在 `feature/auto-upgrade-YYYY-MM-DD` 分支上操作，禁止直接在 master 上提交。
2. **只改进不破坏**：改进内容为新增测试、代码重构、bug 修复、性能优化或新功能；禁止删除既有功能或改变对外接口语义。
3. **可回退**：合并前必须保留完整 commit 历史，合并使用 `--no-ff`，确保可 `git revert` 单 commit 回退。
4. **早间汇报**：每次自动升级完成后，次日 08:30 必须向用户汇报改进内容、风险点与回退方法；未经用户确认不得推送 master。
5. **文档同步**：合并 master 前必须完成 6 份文档版本号统一（约束 3.17）。
6. **测试门禁**：合并前必须 pytest 全通过且 mypy 零错误。

违反上述任一前提时，仍需等待用户明确确认后才能继续。

### 2.2 目录结构约束（新增文件必须遵守）

新增代码文件前，必须先确认当前模块归属层级，禁止随意乱放：

```text
postman_api_tester/
├─ core/                    # 核心执行层（禁止引用 Flask / handlers / services）
│   ├─ batch_scheduler.py        # 依赖感知分批调度器
│   ├─ checkpoint_manager.py     # 断点恢复管理（原子写）
│   ├─ concurrent_executor.py    # 批次内并行执行封装
│   ├─ execution_helpers.py      # 执行辅助（请求发送 / 断言评估 / 数据驱动展开）
│   ├─ html_reporter.py          # HTML 报告生成（Jinja2 模板渲染）
│   ├─ report_engine.py          # 报告引擎（三件套产物生成）
│   ├─ types.py                  # 共享类型定义（TypedDict）
│   └─ variable_context.py       # 请求串联变量上下文
├─ handlers/                # 路由编排层（仅装饰器 + 参数校验 + 调用 services）
│   ├─ base_handler.py           # 路由基类（json_error / json_response 统一封装）
│   ├─ http_handler.py           # HTTP 请求执行（重试 / 代理通用实现）
│   ├─ report_handler.py         # 报告结果业务编排（结果构建 / 人工判定）
│   ├─ server_routes.py          # 健康检查 / 首页 / 报告文件访问 / favicon
│   ├─ page_routes.py            # 页面渲染（report-view / report-index 等）
│   ├─ job_routes.py             # 任务提交 / 状态轮询（run-postman / run-postman-status）
│   ├─ retry_routes.py           # 失败重试（retry-failures / retry-all）
│   ├─ export_routes.py          # 集合导出（export-collection / -stream / export-junit）
│   ├─ test_proxy_routes.py      # 重发 / 代理 / test-token
│   ├─ report_meta_routes.py     # 报告元数据读写（report-meta / manual-cases）
│   ├─ report_result_routes.py   # 报告结果 / 人工判定 / 排除
│   ├─ collection_routes.py      # 集合预览 / 环境列表 / 报告删除 / 对比
│   ├─ collection_editor_routes.py # Collection 编辑器（parse / save / dependency / send）
│   ├─ global_variables_routes.py # 全局 / 环境变量管理
│   ├─ ui_testing_routes.py      # UI 测试主路由（用例 CRUD / 代理 / 录制会话）
│   ├─ ui_recorder_routes.py     # UI 录制器（事件 / 会话 / 导出）
│   └─ ui_execution_routes.py    # UI 执行（执行 / 报告 / 设置 / 截图 / webhook）
├─ services/                # 领域服务层（业务逻辑，禁止直接返回 Flask Response）
│   ├─ base_service.py           # 服务基类
│   ├─ collection_editor_service.py  # Collection 编辑器业务
│   ├─ global_variables_service.py   # 全局 / 环境变量管理
│   ├─ report_analytics_service.py   # analytics 数据服务
│   ├─ report_delete_service.py      # 报告删除
│   ├─ report_export_service.py      # 集合导出（full / report_only / stream）
│   ├─ report_job_execution_service.py # 任务执行编排
│   ├─ report_job_submission_service.py # 任务入队
│   ├─ report_judgement_service.py   # 人工判定
│   ├─ report_junit_service.py       # JUnit XML 导出
│   ├─ report_list_service.py        # 报告列表构建
│   ├─ report_lock_service.py        # 报告并发写锁（RLock）
│   ├─ report_manual_case_service.py # 人工用例 CRUD
│   ├─ report_meta_update_service.py # 元数据写回
│   ├─ report_patch_service.py       # 报告补丁
│   ├─ report_request_service.py     # 报告请求查询
│   ├─ report_results_service.py     # 报告结果构建
│   ├─ report_retry_service.py       # 重试编排
│   ├─ ui_case_store.py          # UI 用例存储
│   ├─ ui_execution_manager.py   # UI 无头执行调度器
│   ├─ ui_execution_store.py     # UI 执行结果存储
│   ├─ ui_headless_engine.py     # Playwright 无头引擎
│   ├─ ui_proxy_service.py       # UI 反向代理（含 Cookie / Set-Cookie 转发）
│   ├─ ui_recorder_inject.py     # UI 录制器注入脚本
│   └─ ui_settings_store.py      # UI 测试设置存储
├─ utils/                   # 通用工具层（纯函数，禁止持有业务状态）
│   ├─ analytics_utils.py        # analytics 查询参数钳制
│   ├─ cache.py                  # 通用 TTL 缓存
│   ├─ collection_utils.py       # Collection 规范化与合并
│   ├─ data_driver.py            # 数据驱动文件加载（CSV / JSON）
│   ├─ extract_utils.py          # JSONPath 轻量提取（安全子集）
│   ├─ file_utils.py             # 文件原子写入（atomic_write_json / sanitize_export_name / safe_report_artifact）
│   ├─ judgment_utils.py         # 可配置结果判定（errCode & message）
│   ├─ logging_utils.py          # 结构化日志 + 告警快照
│   ├─ pre_request_executor.py   # Pre-request 沙箱表达式执行器
│   ├─ report_utils.py           # 报告工具函数
│   ├─ request_builder.py        # 请求体构建（body 多模式）
│   ├─ request_utils.py          # 请求工具函数
│   ├─ response_parser.py        # 响应解析
│   ├─ result_utils.py           # 结果工具函数
│   ├─ security.py               # 敏感头脱敏（sanitize_headers / strip_auth_headers）
│   ├─ server_utils.py           # 服务端通用工具（clamp_page_size / get_local_ip）
│   ├─ url_utils.py              # URL 处理（合并 / 拆解 / 代理改写）
│   ├─ variable_functions.py     # 内置变量函数注册表（12+ 函数）
│   └─ variable_substitution.py  # {{变量}} 替换引擎
├─ templates/               # Jinja2 前端模板（15 个页面）
│   ├─ index.html                  # 报告中心首页
│   ├─ adhoc_run.html              # ad-hoc 接口测试页
│   ├─ collection_editor.html      # Collection 可视化编辑器
│   ├─ report_index.html           # 报告列表页（Jinja2 + {% raw %} 保护 JS 模板字面量）
│   ├─ report_view.html            # 单报告详情页
│   ├─ report_not_found.html       # 报告未找到页
│   ├─ ui_recorder.html            # UI 录制器页面
│   ├─ ui_recorder_demo.html       # UI 录制演示页
│   ├─ ui_testing_index.html       # UI 用例列表首页
│   ├─ ui_testing_recorder.html    # UI 录制页面
│   ├─ ui_testing_editor.html      # UI 用例编辑器
│   ├─ ui_testing_replay.html      # 浏览器回放页面
│   ├─ ui_testing_report.html      # 执行报告详情（进度条 / 筛选 / 分页 / 截图）
│   ├─ ui_testing_reports.html     # UI 执行报告列表
│   └─ ui_testing_settings.html    # UI 测试设置页面
├─ tests/                   # pytest 单元测试（198 个文件，~2100 个测试）
│   └─ test_*.py                 # 按被测模块命名
├─ __main__.py              # 包级启动入口（python -m postman_api_tester，推荐方式）
├─ postman_api_tester.py    # CLI 执行入口
├─ report_server.py         # 路由装配模块（38 条路由，仅装饰器 + 单行委托）
├─ report_server_app.py     # 应用工厂（Flask app 创建与启动）
├─ report_server_config.py  # 配置读取工具（_cfg_bool / _cfg_float / _cfg_tuple）
├─ report_server_utils.py   # 报告服务工具函数（exclude_key / normalize / to_bool）
├─ config.py                # 业务配置常量（token / base_url / 超时 / 开关）
├─ models.py                # 共享数据模型
├─ exceptions.py            # 自定义异常（ValidationError / PostmanTestException）
├─ assertions.py            # 断言引擎（JSONPath / 状态码 / 响应时间）
├─ parser.py                # Postman Collection 解析（ApiConfig TypedDict）
├─ executor.py              # 测试执行器（变量替换 / 提取 / pre-request）
├─ session.py               # HTTP 会话管理（重试 / 超时）
├─ auth.py                  # 认证与 Token 获取
├─ db_feedback.py           # 数据库反馈解析
├─ report_repository.py     # 报告仓储（30s TTL 缓存 / rglob 扫描）
├─ report_meta_repository.py # 元数据读写仓储
├─ report_job_store.py      # 任务状态内存存储（RUN_JOBS_MAX 上限）
├─ runtime_utils.py         # 运行时工具（checkpoint 键复合）
└─ run_test_and_open.py     # 交互式快速启动脚本
├─ uireports/               # UI 测试执行报告存储（不提交 git）
│   └─ exec_{job_id}/
│       ├─ result.json              # 完整执行报告
│       └─ screenshots/             # 失败步骤截图
├─ logs/                   # 日志目录（统一 10 天自动清理）
│   ├─ report_server.log        # 回放 / 导航 / 代理日志（RotatingFileHandler）
│   └─ headless/                # 无头执行请求日志（JSONL）
└─ reports/                # 默认报告输出目录（支持子目录）
```

分层规则：

1. **core/**：执行链路核心逻辑（HTML 报告生成、断点恢复、执行管道、类型定义）。禁止引用 Flask 或 handlers/services。
2. **handlers/**：仅允许接收 HTTP 请求、参数校验、调用 services、返回 Response。禁止直接操作文件系统或发 HTTP 请求。
3. **services/**：承担业务逻辑（导出、重试、列表、判定、锁、提交等）。允许调用 utils 和 repositories，禁止直接返回 Flask Response。
4. **utils/**：纯函数工具（URL 合并、请求构建、响应解析、脱敏、缓存）。禁止持有业务状态，禁止引用 handlers/services。
5. **tests/**：按 `test_<被测模块>.py` 命名。handler 测试必须使用 `test_request_context()`，services 测试允许 mock 依赖。
6. **新增路由**：必须先创建 `handlers/<name>_routes.py`，再在 `report_server.py` 中以 `return _handler_name(args)` 形式注册。
7. **新增服务**：必须创建 `services/<name>_service.py`，如有共享逻辑可抽象为 `base_service.py` 子类。

## 3. 当前有效约束清单（去重归并）

### 3.1 运行与环境约束

1. Python 版本要求：3.10+。
2. 依赖安装入口：`requirements.txt`。
3. 建议在项目根目录执行命令：`d:/tangzk/py/seldom-api-testing`。
4. `POSTMAN_TOKEN`、`POSTMAN_BASE_URL` 支持环境变量注入，优先于 `config.py` 默认值。
5. 报告服务端口/主机支持环境变量：`REPORT_SERVER_PORT`（默认 5000）、`REPORT_SERVER_HOST`（默认 0.0.0.0）。
6. 生产环境优先 `waitress`，未安装时降级 Flask 开发服务器；禁止生产 `debug=True`。

### 3.2 执行参数与安全约束

1. CLI 参数顺序：
   `python -m postman_api_tester.postman_api_tester <postman_file> [base_url] [output_dir] [token] [results_per_page]`
2. token 优先级：命令行/代码入参 > `POSTMAN_TOKEN` > `config.py::TOKEN` > 自动登录。
3. base_url 优先级：命令行/代码入参 > `POSTMAN_BASE_URL` > `config.py::BASE_URL` > 从 Collection 解析。
4. output_dir 优先级：命令行/代码入参 > `config.py::REPORT_OUTPUT_DIR` > 项目根目录 `reports/`。
5. `results_per_page` 默认 30，建议范围 1~100。
6. 请求超时由 `config.py` 集中管理（`REQUEST_CONNECT_TIMEOUT`、`REQUEST_READ_TIMEOUT`），执行层禁止硬编码。
7. token 并发隔离必须保持实例级（`_auth_token`），禁止回退为类变量。
8. URL 解析必须兼容 `request.url.path` 缺失时回退 `request.url.raw`，并支持绝对 URL 与 `{{baseUrl}}/...`。
9. SSRF 防护必须保留：`run_postman_tests`、`/api/run-postman`、`/re-request-api`、`/api/proxy-request`、`/api/run-ad-hoc-tests` 的目标 URL/base_url 仅允许 `http/https`。proxy 端点另须校验 `PROXY_ALLOWED_HOSTS` 域名白名单（非空时）。
10. **UI 代理 SSRF 豁免（v1.30.117）**：`ui_proxy_service.py::fetch_and_rewrite()` 和 `fetch_resource()` 禁止对目标 URL 执行 `is_safe_url()` 私有地址检查。UI 代理的核心用途是转发到用户配置的内网系统（如 `10.x`/`172.16-31.x`/`192.168.x`），SSRF 防护会阻断合法请求导致页面 404。`is_safe_url()` 仅用于接受外部不可控输入的端点。

### 3.3 报告产物约束

每次执行必须生成同名前缀三件套：

1. `postman_report_时间戳.html`
2. `postman_report_时间戳_details.json`
3. `postman_report_时间戳_meta.json`

执行中断（部分成功）时也必须生成三件套，且使用新时间戳，禁止覆盖历史报告。

### 3.4 报告中心核心约束（report_server.py）

1. 默认监听 `0.0.0.0:5000`。
2. 报告目录优先级：`POSTMAN_REPORTS_DIR` > `REPORTS_DIR` > `config.py::REPORT_OUTPUT_DIR` > 项目根目录 `reports/`。
3. 单报告页分页默认 20、上限 100。
4. 支持历史对比（新增/移除/状态变化/成功率变化）。
5. 同报告并发写回必须使用“按报告名粒度”的写锁。
6. 上传文件名入库前必须清洗（仅保留字母/数字/汉字/`-._()（）【】`）。
7. `RUN_JOBS` 上限由 `config.py::RUN_JOBS_MAX` 管理（默认 200），不可移除清理逻辑。
8. `list_reports()` 必须维持 30 秒 TTL 缓存，并在新报告生成时执行 `_invalidate_reports_cache()`。
9. 首页仅加载摘要字段，禁止注入完整 `results/request_info/response_info`。
10. `/health` 必须保留 `status`、`timestamp`，允许扩展 `log_alert` 等运行态字段。
11. 结构化日志必须使用 `logging`，核心路径禁止新增 `print()` 代替 `logger.*`。
12. 页面渲染必须使用 `templates/` 外置模板；不支持内嵌模板回退分支。
13. 报告删除必须限定在 `REPORTS_DIR` 内，并同步清理 `.html`、`_details.json`、`_meta.json`、`_page_*.html`。
14. 报告结果详情允许人工判定（成功/失败/恢复自动），需保留 `manual_judgement/judgement_history` 并同步重算 summary；重试真实回写后取消人工判定激活状态。
15. 日志告警阈值由 `LOG_ALERT_ERROR_WINDOW_SECONDS`、`LOG_ALERT_ERROR_RATE_THRESHOLD_PER_MIN` 集中管理，并通过 `/health.log_alert` 对外可观测。
16. analytics 能力受 `ENABLE_REPORT_ANALYTICS` 集中控制；关闭时页面不注入 analytics 主容器，`/api/report-analytics/<report_name>` 与 `/api/report-analytics-compare` 必须返回 403。
17. analytics 查询参数 `top_n`、`trend_limit` 必须按配置默认值与上限自动钳制；`include_samples` 必须保留布尔兼容解析。
18. analytics compare 必须保持新增接口形态，不得破坏既有 `/api/compare` 语义。
19. analytics 图表可视化受 `ENABLE_REPORT_ANALYTICS_CHARTS` 集中控制，且必须受父开关 `ENABLE_REPORT_ANALYTICS` 联动约束；父开关关闭时，无论子开关为何值，页面均不加载 Chart.js、不渲染图表 canvas。
20. 图表 CDN 加载失败时必须自动降级为纯表格，不得阻断页面其他功能。
21. **路由层提取约束（v1.3.0）**：同约束 2.2 第 6 条。`report_server.py` 仅保留装饰器与单行委托，业务逻辑下沉至 `handlers/*`。
22. **类型检查约束（v1.3.0）**：handlers/ 与 services/ 新增代码必须通过 mypy 检查（实际命令见约束 3.9.4，`pyproject.toml` 中配置了部分 strict 标志但未启用完整 `--strict`）；禁止新增 `Any` 类型扩散，禁止新增未标注返回类型的公有函数。
23. **测试覆盖约束（v1.3.0）**：handlers/ 下新增路由必须配套单元测试，异常路径（400/403/404）与成功路径均需覆盖（测试命名规范见约束 2.2 第 5 条）；禁止在 handler 模块中调用 `configure_run_jobs()` 等全局状态初始化函数。
24. **启动入口约束（v1.4.1）**：报告服务唯一推荐的启动方式为 `python -m postman_api_tester`（通过包级 `__main__.py` 入口调用 `ReportServerApp.run_app(app)`）。禁止直接以 `python postman_api_tester/report_server.py` 或 `python -m postman_api_tester.report_server` 方式启动（`report_server.py` 仅承载路由装配，不提供命令行入口）。根目录兼容启动器 `report_server.py` 已移除。
25. **子目录报告支持约束（v1.3.1）**：报告可生成到 `reports/` 下任意子目录中；`list_reports()` 必须使用 `rglob` 递归扫描，`serve_report()` 必须通过 `meta_file` 推导实际路径并直接返回文件内容。禁止在子目录报告场景下依赖 `send_from_directory`（其内部 `secure_filename` 会将路径分隔符替换为下划线，导致 404）。
26. **报告扫描排除约束（v1.3.1）**：`report_meta_files()` 与 `legacy_postman_html_files()` 扫描时必须按 `REPORT_SCAN_EXCLUDE_DIRS` 过滤排除目录（默认 `"old"`）；排除判断仅检查目录层级（`Path.parts[:-1]`），不得误伤同名文件。
27. **favicon 路由（v1.3.3）**：`/favicon.ico` 必须返回 204 空响应，消除浏览器默认请求导致的 404 日志噪音。

### 3.5 导入、导出与脱敏约束

1. 导出 JSON 必须为单一文件，并按规则合成：原始集合（请求参数回填） + `manual_cases` - `manual_exclusions`。
2. 导出范围支持 `full` 与 `report_only`，默认与开关由 `config.py` 统一管理；禁用 `report_only` 时自动降级 `full`，禁止 500。
3. 导出必须保留完整请求语义：`method/url/headers/params/body`，以及 `body_mode/body_data`。
4. 导入链路必须可解析导出结构（含 `request.url.raw/path/query` 与 body 各模式），保证“可预览可执行”。
5. 敏感头规则由 `config.py::SENSITIVE_HEADERS` 与内置集合合并统一生效：
   - 详情写入时掩码 `***`
   - 导出 `include_auth=false` 时按同一规则剔除
   - 禁止不同链路使用不同敏感头集合
6. 流式导出必须通过新增接口 `/api/export-collection-stream` 提供，不得替换 `/api/export-collection`。
7. 导出通道仅允许 `REPORT_EXPORT_CHANNEL_MODE=auto|legacy|stream`；非法值回退 `auto`。
8. `auto` 模式按 `REPORT_EXPORT_STREAM_THRESHOLD` 分流，流式失败自动回退旧接口；`legacy/stream` 为强制通道。

### 3.6 人工用例、筛选与路由一致性约束

1. 人工用例写入 `_meta.json::manual_cases`，默认目录由 `MANUAL_CASE_FOLDER_NAME` 提供且页面可覆盖填写。
2. `manual_cases` 必须有稳定 `id`，更新/删除按 `id` 定位，禁止用 `name/url` 作为唯一键。
3. 排除能力必须支持“置灰 + 显示/隐藏已排除”开关。
4. 路由层新增关键字参数时，必须同步更新被调函数签名、返回字段、模板消费字段。
5. 执行层 `FAILED/ERROR` 结果必须输出 `db_feedback` 结构化字段，不得降级为仅日志可见。

### 3.7 ad-hoc 执行约束

1. 首页通过“新增接口测试”进入独立页执行，能力受 `ENABLE_ADHOC_RUN`、`ADHOC_MAX_ITEMS`、`ADHOC_DEFAULT_COLLECTION_NAME` 控制。
2. `/api/run-ad-hoc-tests` 仅接收 JSON；`cases` 必须为非空数组，数量不超过 `ADHOC_MAX_ITEMS`。
3. 入队前必须规范化为 Postman Collection v2.1 JSON，写入 `uploaded_collections/<job_id>.json`，并复用现有任务状态与报告链路。
4. ad-hoc URL 仅允许三类：绝对 `http/https`、`{{baseUrl}}/...` 或 `{{base_url}}/...`、配合 `base_url` 的相对路径。
5. body 模式统一支持 `none/raw/urlencoded/formdata/graphql/binary`，不得与导入/重试链路语义漂移。

### 3.8 前端一致性与调试回归约束

1. 所有 UI 改动必须在 `templates/*.html` 实现。
2. 同类页面优先复用既有设计语言；如引入新风格，必须同步评估并更新同类旧页面。
3. 声明“页面已统一”的能力（tooltip/复制/折叠/筛选提示）必须核对全部真实渲染入口，禁止只改单分支即宣告收口。
4. `Ctrl+C/Cmd+C` 接管仅允许在浮层可见且非输入态（input/textarea/select/contenteditable）下生效。
5. 调试前必须先检查目标端口占用，必要时先清理旧进程再启动最新服务；验证后必须关闭本次启动的服务进程。
6. 涉及 `report_server.py` 路由/筛选/导出/人工用例改动，提交前至少验证：
   - `/health`
   - `/`
   - `/report-view`
   - `/api/report-results?include_excluded=true/false`
   - `/api/manual-cases` add/update/delete
   - `/api/export-collection`
   - `/api/report-analytics/<report_name>`
   - `/api/report-analytics-compare`
7. 问题排查默认流程：日志定位 -> 最小复现 -> true/false 双态验证 -> CRUD 闭环 -> 回滚清理。

### 3.9 断点恢复与类型检查约束

1. Checkpoint 恢复键必须使用稳定 `item_path`，禁止使用 `name/url` 去重。
2. Checkpoint 写入必须原子写（tmp + replace）；当前 `core/checkpoint_manager.py` 使用独立的 `NamedTemporaryFile` + `os.replace` 实现，与 `utils/file_utils.py::atomic_write_json()` 模式一致但尚未统一，后续可考虑合并。读取前必须结构校验；校验失败降级全量执行，不中断主流程。
3. `ENABLE_CHECKPOINT_RECOVERY` 默认 `False`，未显式开启时保持与 v1.2.0 等价行为。
4. mypy 必须使用：
   `python -m mypy --config-file pyproject.toml --explicit-package-bases`
5. CI 门禁需持续执行同一 mypy 命令。

### 3.10 方案实施治理约束（61-67 归并）

1. 方案进入实施准备后必须冻结迭代范围并标注 Out of Scope。
2. 新增需求/接口结构调整/跨层重构/性能策略调整需变更单审批后实施。
3. 实施按“阶段目标 -> 门禁验证 -> 下一阶段”推进，门禁失败仅允许修复。
4. 单 PR 仅覆盖单一阶段目标，建议后端 <= 6 文件、前端 <= 3 文件，超限需拆分。
5. 既有稳定接口语义（如 `/api/reports`、`/api/report-results`、`/api/report-meta`、`/api/compare`）不得直接破坏，需新增接口灰度迁移。
6. 新增能力必须提供集中配置开关，默认值优先保持旧行为；开关关闭时页面不注入新能力容器、不发新请求。
7. 每阶段合并前必须沉淀“五段式验收记录”：已完成项/未完成项/风险与遗留/回滚验证/回归结果。

### 3.11 局域网访问约束

1. 本机访问：`http://127.0.0.1:5000`。
2. 局域网访问需放通 5000 端口。
3. 推荐脚本：`allow_report_server_firewall.ps1`。

### 3.12 可配置结果自动判定约束（v1.3.3）

1. **四层优先级**：Web 任务级 `judgment_config` > 集合内接口级 `x_*` 扩展字段 > 全局 `config.py` 配置 > 内置默认值。每层仅覆盖已显式提供的字段，未提供的字段透传上层值。
2. **全局配置项**：`ENABLE_ERR_CODE_JUDGMENT`（默认 `False`）、`SUCCESS_ERR_CODES`（默认 `"0"`）、`ENABLE_MESSAGE_JUDGMENT`（默认 `True`）、`SUCCESS_MESSAGES`（默认 `"success"`）。均支持环境变量覆盖。
3. **集合内覆盖字段**：`x_success_err_codes`、`x_success_messages`、`x_enable_err_code_judgment`、`x_enable_message_judgment`，放置在 Postman Collection item 的 `request` 对象中。
4. **Web 界面填写即启用**：首页上传表单与 ad-hoc 页面提供两个文本输入框（errCode 成功值、message 成功值），填写即覆盖全局配置，不填写则使用全局配置或内置默认值；输入框 placeholder 显示当前默认值。
5. **判定逻辑封装**：`utils/judgment_utils.py` 提供 `evaluate_result_judgment()` 和 `resolve_judgment_params()` 两个核心函数；`executor.py`、`handlers/test_proxy_routes.py` 必须通过 `judgment_utils` 执行判定，禁止在业务层硬编码判定逻辑。
6. **向后兼容**：不配置任何新参数时，行为与 v1.3.2 完全一致（状态码 + message 空/success 判定）；`ENABLE_ERR_CODE_JUDGMENT=False` 时不检查 errCode，保持旧行为。
7. **参数传递链路**：`handlers/job_routes.py` → `services/report_job_execution_service.py` → `postman_api_tester.py::run_postman_tests()` → `core/execution_helpers.py` → `executor.py`；修改 `judgment_config` 参数时必须同步所有转发层签名，避免运行时 `TypeError`。

### 3.13 数据驱动测试约束（v1.4.0）

1. **功能开关**：`ENABLE_DATA_DRIVEN`（默认 `False`），支持环境变量覆盖。开关关闭时不加载数据文件、不展开接口、页面不注入上传入口。
2. **数据文件格式**：仅支持 CSV（UTF-8/UTF-8-sig/GBK）和 JSON（数组对象）。不支持 Excel。
3. **数据文件路径安全**：Web 上传的数据文件存入 `uploaded_collections/`；CLI 模式允许任意路径但必须文件存在。禁止路径穿越。
4. **行数限制**：`DATA_FILE_MAX_ROWS`（默认 10000），超限拒绝执行。`DATA_FILE_MAX_SIZE`（默认 10MB）。
5. **按需展开**：仅 URL/headers/body/params 中引用了数据文件变量名的接口才按数据行数展开；无变量引用的接口保持单次执行。`{{baseUrl}}`/`{{base_url}}` 不算数据驱动变量。
6. **变量替换范围**：`utils/variable_substitution.py` 中的 `substitute_variables()` 排除 `baseUrl` 和 `base_url`；替换范围覆盖 URL/headers/body/params 中的 `{{变量名}}`。
7. **checkpoint 兼容**：展开接口（data_index > 0）的 checkpoint 键为 `item_path#data_index` 复合键（如 `0.2.1#3`）；未展开接口保持原键格式（如 `0.2.1`），向后兼容旧 checkpoint。
8. **报告展示**：结果记录含 `data_index` 字段（未展开接口 data_index=0）；报告列表在接口名称后显示数据组编号徽标（`#N`）。
9. **参数传递链路**：`handlers/job_routes.py` → `services/report_job_submission_service.py` → `services/report_job_execution_service.py` → `postman_api_tester.py::run_postman_tests()` → `core/execution_helpers.py::_prepare_execution_apis()`；`data_file` 参数必须同步所有转发层签名。

### 3.14 请求串联与变量提取约束（v1.4.0）

1. **功能开关**：`ENABLE_VARIABLE_EXTRACTION`（默认 `False`），支持环境变量覆盖。开关关闭时不创建 VariableContext、不解析 `x_extract`、页面不注入编辑区。
2. **变量上下文生命周期**：`core/variable_context.py::VariableContext` 仅在 `_execute_api_suite()` 调用期间有效，不持久化到 checkpoint。
3. **x_extract 配置**：放置在 Postman Collection item 的 `request` 对象中，键为变量名，值为 JSONPath 表达式（`$.field.subfield`、`$.array[0].field`）或特殊前缀 `$header.Name`（提取响应头）。
4. **JSONPath 安全**：`utils/extract_utils.py` 仅支持简化路径（字段访问 + 数组索引），禁止 eval/exec/过滤器/通配符/递归下降。使用自研轻量解析器。
5. **变量优先级**：数据行变量（已烘焙到 ApiConfig）> 运行时提取变量（VariableContext）> Collection 预置变量 > initial_variables。
6. **提取失败行为**：提取失败时变量不写入上下文（不覆盖已有值），日志记录警告，报告中 `extracted_variables` 仅记录成功提取的键值对。
7. **选择性执行警告**：选择性执行可能跳过"变量生产"接口，导致后续接口变量缺失。提供 `initial_variables` 参数让用户预填关键变量。
8. **executor 集成**：`executor.py::execute_test()` 在请求前调用 `substitute_in_api_config()` 替换变量，请求后调用 `variable_context.update_from_extract()` 提取变量。
9. **ad-hoc 支持**：`utils/collection_utils.py::normalize_adhoc_case()` 支持 `x_extract` 字段，`build_adhoc_collection()` 将其写入 request 节点。

### 3.15 Collection 可视化编辑器约束（v1.5.0）

1. **路由层**：`handlers/collection_editor_routes.py` 提供 4 条 API（parse/save/dependency/send），`services/collection_editor_service.py` 承载全部业务逻辑，路由层仅做参数校验和异常捕获。
2. **body_data 双格式映射**：编辑器层 body_data 格式为 `{content, language}`，执行器层期望 `{raw_content, raw_language, raw_content_type}`。`send_single_request()` 中必须做字段映射，不可直接透传。
3. **变量替换**：`send_single_request()` 对所有文本字段（url/headers/params/body）使用 `substitute_variables()` 做 `{{var}}` 替换，未匹配变量保留原值（`{{unknown}}` 不报错）。
4. **前端按钮 type**：所有 `<button>` 必须设置 `type="button"`，防止默认 submit 行为导致页面刷新。
5. **运行时变量池**：前端维护 `runtimeVars` 对象，Send 后自动执行 x_extract 提取，后续 Send 通过 `collectVariables()` 合并预置变量和运行时变量。
6. **单测覆盖**：`tests/test_collection_editor_service.py` 至少覆盖 parse/build/dependency/validate/send 五个核心函数的正常路径与异常路径。

### 3.16 错误码体系约束（v1.5.0）

1. **命名规则**：错误码格式为 `模块前缀_序号`，如 `CE_PARSE_001`。前缀对照：`CE_`（Collection Editor）、`COL_`（Collection 预览/导出）、`JOB_`（任务执行）、`RPT_`（报告相关）、`HTTP_`（代理/重发请求）、`COM_`（通用错误）。
2. **base_handler.json_error()**：签名 `json_error(message, status_code, error_code="")`，所有 handler 中的 `_json_error()` 调用必须传递 error_code 参数。
3. **响应结构**：错误响应包含 `error_code` 字段，前端显示格式为 `[错误码] 提示信息`。
4. **500 错误消息**：catch-all 块使用 `type(e).__name__` 而非 `str(e)`，避免泄露内部信息。
5. **前端网络错误**：catch 块中的网络错误统一显示 `COM_NET_001`。
6. **错误码速查表**：`操作手册.md` 第 14 节维护完整的错误码清单，新增错误码时必须同步更新。

### 3.17 版本号同步约束（v1.5.0）

1. **版本发布时**：必须同步更新以下文件的版本号：
   - `postman_api_tester/__init__.py` → `__version__`
   - `postman_api_tester/README.md` → 顶部版本号 + 新增重点章节
   - `postman_api_tester/操作手册.md` → 顶部版本号
   - `postman_api_tester/最终交付清单.md` → 顶部版本号 + 新增功能条目
   - `postman_api_tester/开发阅读文档.md` → 版本基线
   - `postman_api_tester/快速命令参考.md` → 顶部版本号 + 发布信息
2. **版本号格式**：语义化版本 `MAJOR.MINOR.PATCH`，功能发布递增 MINOR，修复递增 PATCH。
3. **git push 前检查**：推送前必须确认所有文档版本号一致。

### 3.18 可视化编辑器增强约束（v1.6.0）

1. **页内执行面板**：`executeCollection()` 不得跳转页面，必须通过模态框轮询 `/api/run-postman-status/<job_id>` 展示进度。后端终态为 `success`（非 `done`），轮询判断须匹配。
2. **报告链接格式**：执行完成后报告链接使用 `/report-view?name=<report_name>`，不是 `/reports/<name>/index.html`。
3. **拖拽排序**：`renderGroup()` 中每个接口项必须设置 `draggable="true"` 和 `data-req-id` 属性。拖拽后须同步更新 `state.collection.groups` 和 `state.allRequests`。
4. **Body 变量替换**：`executor.py` 中，当 body 为字符串且以 `{` 或 `[` 开头时，在变量替换后尝试 `json.loads()` 重新解析，确保 requests 库发送 JSON 对象而非字符串。
5. **快速命令参考**：`postman_api_tester/快速命令参考.md` 顶部版本号与发布信息必须与其他文档同步。

### 3.19 变量自动补全约束（v1.7.0）

1. **触发机制**：在 `input[type="text"]` 和 `textarea` 中监听 `input` 事件，检测光标前是否有 `{{` 且未闭合 `}}`。仅当 `{{` 后为 `[a-zA-Z_0-9.]*` 时触发。
2. **变量来源**：`collectAllVariables()` 从三个来源汇总——Collection 预置变量、`state.runtimeVars` 运行时变量池、所有接口的 `x_extract` 键。使用 `Set` 去重，先入为主。
3. **插入行为**：选中后替换 `{{...光标...` 为 `{{变量名}}`，光标定位到 `}}` 之后，并触发 `change` 事件同步到 `editBuffer`。
4. **键盘交互**：↑/↓ 切换选中项，Enter/Tab 确认，Escape 关闭。点击外部区域自动关闭下拉框。
5. **z-index 层级**：下拉框 `z-index: 2000`，高于执行模态框（1000），避免被遮挡。

### 3.20 可视化编辑器接口管理增强约束（v1.5.0，原 v1.8.0）

1. **纯前端实现**：7 项新增功能（空白接口新增、接口复制、cURL 导入、HAR 导入、Postman 代码片段导入、新建文件夹、接口跨文件夹拖拽）全部在 `templates/collection_editor.html` 中通过 JavaScript 实现，无后端变更。
2. **分组路径系统**：每个 group 使用唯一路径标识（如 `"0"`, `"0.1"`, `"0.1.2"`），`findGroupByPath()` 和 `findRequestLocation()` 通过路径定位分组。路径与 `renderGroup()` 的递归深度对应。
3. **下拉菜单统一入口**："+ 新增"下拉菜单包含 5 种导入方式和 2 种复制方式，无选中接口时复制项置灰。"新建文件夹"为独立按钮。
4. **模态框 z-index**：功能模态框 `z-index: 1500`，高于执行模态框（1000）但低于变量自动补全（2000）。右键菜单 `z-index: 2500`。
5. **cURL 解析**：前端 `parseCurlCommand()` 支持 20+ 参数，tokenize 函数处理引号内空格。高级参数（`--proxy`/`--cert`/`--cookie-jar`）记录到 description 字段。
6. **HAR 响应提取**：导入时将响应 JSON 的**顶层字段**自动生成 `x_extract` 规则（变量名 = 字段名，JSONPath = `$.字段名`），不展开嵌套结构。
7. **文件夹拖拽**：文件夹标题设置 `draggable="true"`，拖拽使用独立事件处理函数（`handleFolderDrag*`），与接口拖拽通过 `dragType` 变量区分。循环引用检测：不允许拖入自身子文件夹。
8. **多选拖拽**：拖拽已选中接口时移动所有选中项。`moveRequestsToGroup()` 先按分组收集移除位置，倒序移除避免索引偏移，再计算目标插入位置。
9. **所有按钮 `type="button"`**：新增模态框中的所有按钮均设置 `type="button"`，防止默认 submit 行为（约束 3.15.4）。
10. **导出兼容**：所有新增方式最终写入 `state.collection.groups`，通过 `syncAllRequestsFromCollection()` 同步 `state.allRequests`，导出时由后端 `build_collection_json()` 自动包含。

### 3.21 Pre-request 脚本约束（v1.18.0）

1. **功能开关**：`ENABLE_PRE_REQUEST_SCRIPT`（默认 `False`），支持环境变量覆盖。开关关闭时 `x_pre_request` 字段被忽略，不影响正常执行。
2. **两层能力**：
   - **Step A（内置函数扩展）**：6 个新函数（`hmac_sha256`/`md5`/`base64_encode`/`base64_decode`/`random_string`/`url_encode`）通过 `@register()` 注册到 `utils/variable_functions.py`，无需额外配置即可在 `{{func(args)}}` 语法中使用。
   - **Step B（x_pre_request 表达式）**：需启用 `ENABLE_PRE_REQUEST_SCRIPT`，在请求发出前执行 Python 表达式，结果作为局部变量参与本次请求的变量替换。
3. **沙箱安全策略**（`utils/pre_request_executor.py`）：
   - **限制内置函数**：`__builtins__` 设为安全子集（`int`/`str`/`float`/`len`/`range`/`list`/`dict` 等），禁用 `open`/`exec`/`eval`/`import`。
   - **白名单模块**：仅允许 `hashlib`/`hmac`/`base64`/`time`/`json`/`re`，禁止网络/文件/系统调用。
   - **危险关键字拦截**：正则检查 `import`/`__import__`/`exec`/`eval`/`compile`/`open`/`os`/`sys`/`subprocess`/`__builtins__`/`globals`/`locals` 等关键字。
   - **超时保护**：表达式执行超时 1 秒。Unix 使用 `signal.SIGALRM`，Windows 使用 `ThreadPoolExecutor`。
   - **错误处理**：表达式执行失败返回空字符串，不中断请求流程，日志记录具体错误信息。
4. **变量隔离**：pre-request 设置的变量仅在当前请求的变量替换中使用（局部变量），**不写入全局 `VariableContext`**。避免并发执行时变量互相覆盖，不影响批次调度器的依赖计算。
5. **执行流程**：`executor.py::execute_test()` 中，先执行 `x_pre_request` 表达式得到 `local_vars`，再合并到 `merged_vars = {**variable_context.variables, **local_vars}`，最后调用 `substitute_in_api_config()`。
6. **解析器扩展**：`parser.py` 中 `ApiConfig` TypedDict 新增 `x_pre_request: Optional[Dict[str, str]]` 字段，`_parse_request()` 解析该字段（遵循 `x_*` 扩展模式）。
7. **前端 UI**：`templates/collection_editor.html` 新增 `Pre-request` 标签页，键值对编辑器支持添加/编辑/删除表达式，展示可用模块与变量引用说明。
8. **表达式引用**：可通过 `variables.get('name', 'default')` 引用已有变量，通过 `result.get('var', '')` 引用同批次前置表达式结果（按定义顺序）。
9. **参数传递链路**：`config.py::ENABLE_PRE_REQUEST_SCRIPT` → `executor.py::execute_test()` 条件检查 → `utils/pre_request_executor.py::execute_pre_request()` → 局部变量合并 → `utils/variable_substitution.py::substitute_in_api_config()`。

## 4. 每次修改后更新本文件的命令

在项目根目录执行以下 PowerShell 命令，将“本次改动中疑似约束变更”的候选行追加到本文件末尾，作为待确认记录：

```powershell
$doc = "postman_api_tester/开发阅读文档.md"
$ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
$pattern = "优先级|默认|必须|限制|上限|下限|分页|token|base_url|report_output_dir|POSTMAN_REPORTS_DIR|REPORTS_DIR|5000|status|meta|details"
$files = @(
  "postman_api_tester/config.py",
  "postman_api_tester/postman_api_tester.py",
  "postman_api_tester/__main__.py",
  "postman_api_tester/report_server.py",
  "postman_api_tester/README.md",
  "postman_api_tester/操作手册.md",
  "postman_api_tester/快速命令参考.md",
  "postman_api_tester/最终交付清单.md"
)
"`n## 约束更新记录（$ts）`n" | Add-Content $doc
rg -n -i $pattern $files | Add-Content $doc
"`n" | Add-Content $doc
```

执行后请人工二次整理：

1. 删除误匹配行。
2. 将真实变更写入“当前有效约束清单”对应章节。
3. 保持优先级、默认值、边界值三类信息准确。
4. 保证“约束更新记录”按时间升序维护。

## 5. 建议的开发收尾检查

每次提交前至少确认：

1. `README.md` / `操作手册.md` / `快速命令参考.md` / `最终交付清单.md` 与代码行为一致。
2. 报告目录优先级、token/base_url 优先级未被破坏。
3. 报告三件套仍可完整生成。
4. 本文档已更新到最新约束状态。
5. 四份关联文档版本号与发布日期一致，并同步新增能力说明。
6. 进入收尾阶段时，需补齐阶段验收记录，至少覆盖：已完成项、未完成项、风险与遗留、回滚验证、回归结果。

## 6. 约束更新记录（精简版）

> 本章节仅保留关键里程碑，详细变更请以 `git log` 与版本 commit 消息为准。
> 早期（v1.2.x ~ v1.18.x）的逐项记录已压缩，保留阶段性成果摘要。

### v1.2.0 ~ v1.2.5（2026-05 报告中心基础）
- 报告三件套、人工用例、重试、多环境、断言扩展、流式导出、结果分块渲染。
- analytics 洞察能力（含图表可视化 + 父/子开关联动）。
- 完成 handlers/services/utils 分层迁移收口 + 敏感头脱敏统一。

### v1.3.0 ~ v1.3.4（2026-05 ~ 2026-06 代码质量走查）
- 路由深度提取：report_server.py 1324 → 272 行，33 条路由下沉至 handlers。
- mypy strict 全通过、42 个 handler 单元测试补齐、SSRF 域名白名单、Jinja2 模板安全加固。
- 可配置结果自动判定（errCode & message）四层优先级。
- 薄层 handler 合并（删除 adhoc_handler 等 6 个导出层），测试数 100 → 638。

### v1.4.0 ~ v1.4.1（2026-06-12 数据驱动 + 变量提取）
- 数据驱动测试（CSV/JSON）、请求串联与 `x_extract`、`VariableContext`、checkpoint 复合键。
- 启动入口迁移至 `python -m postman_api_tester`，移除根目录兼容启动器。

### v1.5.0 ~ v1.7.0（2026-06-15 ~ 2026-06-16 编辑器与测试覆盖）
- Collection 可视化编辑器（parse / save / dependency / send + cURL / HAR / 代码片段导入）。
- 错误码体系（`base_handler.json_error()` + 模块前缀）+ 版本号同步约束。
- 变量自动补全（`{{var}}` 输入时下拉提示）+ 接口跨文件夹拖拽。
- 测试大规模强化：638 → 1217。

### v1.8.0 ~ v1.11.0（2026-06-16 ~ 2026-06-17 并发引擎与变量体系）
- 并发执行引擎（依赖感知分批调度 + 批次内并行）。
- 断言引擎扩展（JSONPath / 响应时间 / 状态码多模式）。
- 内置变量函数扩展至 12+、多环境变量管理、跨 scope 覆盖提示。

### v1.12.0 ~ v1.18.0（2026-06-17 ~ 2026-06-25 Pre-request 脚本）
- 代码质量重构 + JSON tree 搜索 + 请求 / 响应对比视图。
- Pre-request 脚本沙箱执行器（`hashlib/hmac/base64/time/json/re` 白名单，超时保护）。

### v1.19.0 ~ v1.20.x（2026-06-25 ~ 2026-07-02 编辑器快捷键与竞品借鉴）
- JSON tree 搜索修复、请求 / 响应对比视图。
- Collection 编辑器快捷键体系 + 批量编辑 / JSONPath 过滤 / 响应搜索。

### v1.31.0 ~ v1.33.9（2026-08 Web UI 测试完善 + 代码走查优化）
- v1.32.0：回放体验优化 — 回放页返回按钮直达列表页、用例列表页一键执行+清除登录信息、移除 new_tab 红色调试覆盖层。
- v1.33.0：代码走查优化（8 项）— C-1 `_active_jobs` 加锁、H-2 Cookie 日志脱敏、C-3 变量存储 I/O 提取到 utils 消除 core→services 跨层引用、M-4 proxy 白名单公共函数抽取、H-3 handlers 文件 I/O 下沉、M-3 录制会话管理类合并、C-2 report_server SPA 兜底函数迁移到 handlers 层。
- v1.33.1：代码走查优化续（5 项）— H-4 `_get_proxy_session_id` 拆分 6 函数、H-5 `ui_testing_proxy` 拆分 4 函数、M-5 `@handle_api_errors` 装饰器、L-3 魔法数字提取常量、L-6 Jinja2 autoescape=True。
- v1.33.2：每晚自动升级 — M-9 网络请求捕获链路补全（两阶段事件模型 + 响应数据回填）、ruff 检查通过。
- v1.33.3：回放状态残留修复 — `savedReplayState` + `sessionStorage` 在 `loadJobData()` 中清除，解决多次执行只运行最后几步的问题；用例列表页增加"查看录制"入口。
- v1.33.4：截图显示修复 — GET handler 支持 HTML 格式截图（与 POST 保存格式对齐），前端根据 Content-Type 自动选择 `<img>` 或 `<iframe>` 渲染。
- v1.33.5：每晚自动升级 — `from __future__ import annotations` 补全（`postman_api_tester.py`、`ui_execution_routes.py`）、UP015 冗余 open 参数移除、UP037 引号类型注解清理；ruff 检查通过。
- v1.33.6：每晚自动升级 — M-7 `@require_global_variables_enabled` 装饰器模式优化，6 个全局变量路由函数使用装饰器替代重复检查。
- v1.33.7：switch_tab 标签页切换录制与回放 — Chrome 扩展录制时通过 `chrome.tabs.onActivated` 自动捕获标签页切换动作，回放引擎和无头引擎均支持 `switch_tab` 动作。
- v1.33.8：每晚自动升级 — 新增 ui_execution_routes 单元测试（7 个），覆盖设置获取/更新/重置、执行状态查询、执行列表等 API。
- v1.33.9：每晚自动升级 — mypy 类型错误清零（42 → 0），修复 9 个文件共 42 处类型错误。
- v1.33.10：每晚自动升级 — mypy 跨平台修复，修复 5 处 type: ignore 在 Windows 与 Linux 上的差异。
- v1.33.11：每晚自动升级 — L-3 魔法数字提取常量（无头引擎超时值：DEFAULT_TIMEOUT_MS、NAVIGATION_TIMEOUT_MS、PAGE_LOAD_TIMEOUT_MS 等），提高代码可读性和维护性。
- v1.33.12：每晚自动升级 — UI 断言增强：新增 6 种断言动作（`assert_title`、`assert_count`、`assert_value`、`assert_enabled`、`assert_disabled`、`assert_not_visible`），覆盖页面标题、元素数量、输入框值、启用/禁用状态、不可见等验证场景。
- v1.33.13：每晚自动升级 — UI 断言增强第二批：新增 2 种断言动作（`assert_attribute`、`assert_toast`），覆盖元素属性验证和 toast/通知消息内容验证。
- v1.33.14：每晚自动升级 — UI 断言增强第三批：新增 4 种断言动作（`assert_css`、`assert_checked`、`assert_dialog`、`assert_cookie`），覆盖 CSS 属性、复选框状态、浏览器对话框、Cookie 验证场景。
- v1.33.15：浏览器回放截图修复 — 修复浏览器回放模式下步骤勾选"截图"后未执行截图的问题，回放引擎 (`ui_recorder_inject.py`) 现在会检查 `step.screenshot` 标志并正确捕获步骤截图。
- v1.33.16：截图状态区分 — 修复成功步骤和失败步骤截图文件名冲突问题。后端截图保存接口 (`ui_execution_routes.py`) 现在根据 `status` 参数区分文件名：成功步骤保存为 `step_N.html`，失败步骤保存为 `step_N_fail.html`；前端回放引擎 (`ui_recorder_inject.py`) 和回放页面 (`ui_testing_replay.html`) 传递步骤状态参数。
- v1.33.17：每晚自动升级 — 新增截图保存接口单元测试（10 个），覆盖成功/失败步骤截图保存、向后兼容性、边界条件、GET 接口等场景。
- v1.33.18：修复重复代码 bug — 清理 v1.33.16 中 `ui_recorder_inject.py` 元素未找到时的重复回调代码。
- v1.33.19：截图字段同步修复 — 回放引擎在截图保存后设置 `result.screenshot = 'saved'`，回放页面通过 `captureScreenshot` 更新步骤结果并上报后端，确保 `result.json` 中的 `screenshot` 字段正确标记，报告页面能显示成功步骤的截图链接。
- v1.33.20：PNG 截图格式 — 后端截图保存接口改用 Playwright 将 HTML 快照转换为 PNG 静态图片，避免 iframe 中 JavaScript 重新执行导致的"动态图"问题；PNG 转换失败时自动回退到 HTML 格式，GET 接口按优先级返回 PNG/HTML 文件。
- v1.33.21：纯文本断言 `assert_text_exists` — 新增无需选择器的断言动作，用户只需输入文字即可验证页面是否包含该文本；无头引擎 (`ui_headless_engine.py`) 使用 `page.get_by_text()` 实现，回放引擎 (`ui_recorder_inject.py`) 使用轮询 `document.body.innerText` 实现自动等待。
- v1.33.22：代码质量优化 — `ui_testing_routes.py` 硬编码端口 `5000` 改用 `config.REPORT_SERVER_PORT` 常量（L-3 收尾）；登录按钮检测从纯中文字符匹配扩展为中英文双语支持 + `is_login_step` 元数据优先（L-8）；新增 `_is_login_step()` 辅助函数（12 个单元测试覆盖）。
- v1.33.23：`assert_text_exists` 诊断增强 — 失败错误信息扩展为 500 字预览 + URL + frame 数量；返回 `match_count` 匹配数量字段。
- v1.33.24：`assert_text_exists` iframe 支持 — 新增 `_count_text_in_all_frames()` / `_getAllText()` 遍历主框架和所有 iframe 查找文本；断言失败自动截图；错误信息包含完整诊断内容。
- v1.33.25：`assert_text_exists` 成功截图 — 修复浏览器回放模式下勾选截图但成功步骤未截图的问题；多匹配时在 `result.error` 字段记录提示（报告显示）。
- v1.33.26：统一截图策略 — 失败步骤自动截图（无论是否勾选截图），两种执行模式（浏览器回放 + 无头引擎）行为一致；消除失败且勾选截图时的重复截图；`result.json` 中 `screenshot` 字段正确标记。
- v1.33.27：语义化选择器断言 `assert_element_exists` — 支持 `button:查看订单` 格式（`role:text`），按元素角色+文字精确断言；支持 25 种 ARIA 角色；无头引擎 `_parse_semantic_selector()` 解析 + `_action_assert_element_exists()` 执行；回放引擎 JS 实现含 iframe 遍历和多匹配提示；20 个单元测试覆盖。

### v1.21.0 ~ v1.30.65（2026-07 至今 Web UI 测试模块 + 代理链路）
- Web UI 测试全链路：浏览器回放、无头执行（Playwright）、设置界面、Webhook、过期清理。
- 代理链路关键修复：new_tab 跨页面状态恢复、重定向 Location 改写、Set-Cookie 转发、session cookie 替换、Token 自动跟踪注入。
- SPA 同域导航判断（从 `_proxy_url` 提取原始 origin）+ 元素未找到自动截图 + 拖拽排序。
- v1.30.64–v1.30.67：未使用导入清理（73 个）、`sanitize_export_name` 路径修复、mypy 类型注解优化（8 → 0 错，94 源文件全通过）；开发阅读文档走查精简（852 → 524 行）。
- v1.30.68：UI 回放停止/暂停按钮修复 — 停止按钮页面加载后即可用；`stopReplay()` 去掉 `isRunning` 守卫；新增 `replayFinished` 标志；引擎 `_waitForElement` 暂停时冻结超时计时。
- v1.30.69：回放计时器暂停同步 — `getElapsedMs()` 扣除暂停时长，计时器、汇总、后端上报统一使用。
- v1.30.70：代理 JS 动态 import 改写 — `_rewrite_js_imports` 改写 `import()` 和 `new URL()` 相对路径为代理资源 URL；`fetch_resource` 对 JS 内容自动应用改写。
- v1.30.71：代码质量优化 — F841 未使用变量（3 处）、F401 未使用导入（2 处）、F541 空 f-string（2 处）、B904 异常链缺失（1 处）、SIM118 dict.keys() 冗余（3 处）、W291/W293/W292 空白（27 处）、UP009/UP015/UP024（10 处）。
- v1.30.72：代码质量优化 — E713 成员检测（1 处）、B007 未使用循环变量（2 处）、UP037 引号注解（2 处）、SIM110 `any()` 替代循环（1 处）、SIM102 合并嵌套 if（1 处）。
- v1.30.73：代码质量优化 — F823 回归修复（恢复 `postman_api_tester.py` 引号注解）、SIM105 `contextlib.suppress` 改写 6 处（auth/executor/report_junit/report_job_execution/ui_headless_engine）。
- v1.30.76：Vite SPA 回放修复 — `_rewrite_js_imports` 扩展依赖数组路径匹配至 `js/` 和 `css/` 目录（原仅 `assets/`），新增 Vite base path 函数 `return"/"+e` 补丁防止双重 `/` 前缀，修复主应用 vendor chunk 404。
- v1.30.77：Vite 嵌入 link 标签改写 — `_rewrite_js_imports` 新增 JS 内嵌 `<link rel="modulepreload" href="...">` HTML 字符串的 href 属性改写，覆盖 Vite preload helper 运行时动态创建的 modulepreload 元素。
- v1.30.78：代码质量优化 — ruff 静态分析修复：RUF100 unused noqa（7 处）、RUF010 显式 f-string 转换（2 处）、PIE810 合并 startswith/endswith 为 tuple（5 处）、FLY002 join→f-string（1 处）、FURB167 正则别名 `re.S`→`re.DOTALL`（1 处）、SIM115 noqa 标注（1 处）。
- v1.30.79：修复 3 个预存 flaky 测试 — `ui_recorder_routes.py` 导出接口补充 `version` 和 `session_id` 字段（测试期望但接口遗漏）；`test_logging_utils.py` 按 `baseFilename` 精确匹配 handler（消除全局 root logger 状态干扰）。pytest 2105 全绿。
- v1.30.80：Vite 副作用导入改写 — `_rewrite_js_imports` 新增 `import"./chunk.js"`（无 `from` 关键字）副作用导入改写为 proxy-resource URL。修复 Vite chunk 文件中 vendor 依赖 404 导致页面灰屏、置灰遮罩和 new_tab 导航后持续加载。
- v1.30.112：UI 回放 new_tab 跨系统跳转里程碑修复（基础平台 → 菜谱系统，`http://10.50.11.120:9101` → `http://10.50.11.120:9301`）—
  ① 删除跨域等待 window.open 逻辑（页面通过 API + 直接导航跳转，从不调用 window.open），直接用 tab_url 导航；
  ② new_tab 步骤完成时机修正：从"开始时立即标记完成"改为"_doNavigate 触发导航后才标记完成"，确保父页面等导航完成后才启动下一步；
  ③ pushState/replaceState URL 构造修复：从 `_r.href`（不含 _proxy_url 的代理 URL）改为从 `_targetLoc.href` 提取原始 _proxy_url 值构造新 URL，SPA 同域导航不再整页刷新；
  ④ `_targetLoc.href` 更新逻辑修正：从 `_r.href` 改为构造后的完整 URL（含 _proxy_url），确保后续 replaceState 能正确提取原始目标 URL；
  ⑤ localStorage JSON 解析严格化：`v.charAt(0)==="{"` 改为 `v.indexOf('{"')===0`，避免非标准 JSON（如 `{user:"xxx"}`）触发 parse 错误。
  **维护确认**：后续修改 `ui_proxy_service.py` 的 pushState/replaceState 拦截逻辑、`ui_recorder_inject.py` 的 new_tab handler 或 `_targetLoc` 状态管理时，必须验证：① 跨系统 new_tab 跳转成功 ② 目标系统内 SPA 同域导航不整页刷新 ③ replaceState URL 中 _proxy_url 参数值正确。
- v1.30.113：代码质量优化 — pathlib 迁移完成（PTH 规则全量修复）—
  ① 修复 120+ 处 PTH 规则问题（PTH123/PTH118/PTH108/PTH110/PTH119/PTH120/PTH100/PTH103/PTH122/PTH117/PTH113），核心模块全面迁移到 pathlib；
  ② 修复 BOM 检测逻辑：恢复 UTF-8-sig BOM 字节检测（`b"\xef\xbb\xbf"`），确保 CSV 首列不含 BOM 字符；
  ③ 补充 Path 导入：parser.py、test_runtime_utils.py、test_parser.py、test_pre_request_integration.py、test_data_driver.py；
  ④ mypy 零错误，2119 pytest 全绿。
- v1.30.114：安全加固 — UI 代理 SSRF 防护 —
  ① 新增 `is_safe_url()` 函数到 `utils/security.py`：检测内网地址（127.0.0.1/localhost/10.x/172.16-31.x/192.168.x/[::1]），使用 `ipaddress` 模块验证私有 IP 段；
  ② 集成到 `ui_proxy_service.py::fetch_and_rewrite()`：请求前验证 URL 安全性，禁止访问内网地址；
  ③ 新增 10 个 SSRF 防护测试用例（公网/私有 IP/localhost/IPv6/无效 URL），覆盖所有边界场景；
  ④ mypy 零错误，2129 pytest 全绿。
  **维护确认**：后续修改 `ui_proxy_service.py` 的代理请求逻辑或 `utils/security.py` 的 URL 安全检查时，必须验证：① 内网地址被正确拦截 ② 公网地址正常访问 ③ 新增测试全部通过。
  **已回退（2026-08-03）**：`fetch_and_rewrite()` 中的 `is_safe_url()` 检查已移除。原因：UI 代理的核心用途是访问内网系统（如 `10.50.11.120:9101`），SSRF 防护阻止了所有 RFC 1918 私有地址（10.x/172.16-31.x/192.168.x），导致登录页 404、回放全部失败。`is_safe_url()` 函数本身保留在 `utils/security.py`，供其他需要 SSRF 防护的场景使用。
- v1.30.115：代码质量优化 — ruff E402 noqa 标注修复 —
  ① 修复 12 处 E402（module level import not at top of file）规则问题；
  ② 涉及文件：postman_api_tester.py、report_server.py、report_server_config.py、run_test_and_open.py、utils/report_utils.py、utils/request_utils.py、utils/response_parser.py、utils/result_utils.py、utils/url_utils.py；
  ③ 均为 intentionally deferred imports（sys.path 操作、配置加载、循环依赖规避等场景），添加 noqa: E402 标注消除 ruff 误报；
  ④ mypy 零错误，2129 pytest 全绿。
- v1.30.116：每晚自动升级 — ruff 检查通过，无代码变更 —
  ① ruff check 零错误，跳过修复步骤；
  ② 仅递增版本号（v1.30.115 → v1.30.116）和更新日期（2026-07-31 → 2026-08-03）；
  ③ mypy 零错误，2129 pytest 全绿。
- v1.30.117：UI 代理 SSRF 防护回退 — 修复内网回放 404 —
  ① 移除 `ui_proxy_service.py::fetch_and_rewrite()` 中的 `is_safe_url()` 检查；
  ② 原因：UI 代理的核心用途是访问内网系统（如 `10.50.11.120:9101`），v1.30.114 的 SSRF 防护阻止了所有 RFC 1918 私有地址，导致登录页返回 404、回放步骤全部 element_not_found；
  ③ `is_safe_url()` 函数保留在 `utils/security.py`，供其他需要 SSRF 防护的场景使用；
  ④ 2129 pytest 全绿，ruff 零错误。
  **经验教训**：安全防护不能一刀切应用到所有链路。UI 代理是用户主动配置的内网目标转发，其本身就是"访问内网"的合法场景。SSRF 防护应仅用于接受外部输入 URL 的端点（如 API 代理、webhook），而非用户已知且受控的代理目标。

- v1.31.8：new_tab SSO token 注入 —
  ① **修复 — new_tab 降级路径 token 注入**：new_tab 走 resolved_url 降级路径时，读取 9101 父系统 localStorage 中的 esp-web:user token，注入到 9301 子系统 localStorage 后刷新页面，解决 SPA 异步认证检查失败导致重定向回登录页的问题；
  ② **新增 — localStorage 诊断数据**：new_tab 步骤 _diag 增加 esp_web_user 读取和 token_injected 标记，记录 token 传递状态。

- v1.31.7：new_tab 诊断增强 —
  ① **新增 — window.open 弹窗触发**：resolved_url 分支改为 window.open 触发弹窗（模拟成功路径），捕获后切换页面，未捕获则降级 page.goto；
  ② **新增 — localStorage/sessionStorage 诊断**：new_tab 步骤 _diag 增加 storage 字段，记录所有 key 列表和 token/access_token/auth_token 值。

- v1.31.6：new_tab 重定向检测与执行终止 —
  ① **修复 — new_tab 重定向立即检测**：new_tab 后页面被重定向到登录/错误/首页时，立即终止执行（不再等待 30 秒超时）。新增 `_execute_step_monitored` 方法每 2 秒轮询 URL，检测到变更立即返回失败；
  ② **新增 — 三层重定向检测**：pre-check（步骤前 URL 匹配）+ framenavigated 监听（实时检测 SPA 路由变化）+ post-check（步骤后 URL 匹配 + SPA history hook）；
  ③ **新增 — SPA history hook 注入**：new_tab 通过后在页面注入 `window.__headless_redirect_url` 监控，拦截 `pushState`/`replaceState` 检测登录/错误页路由；
  ④ **修复 — _urls_match 端口比对**：之前仅比对 hostname，SPA 跨端口重定向（如 9301→9101）漏检；改为 `netloc`（hostname:port）比对；
  ⑤ **新增 — 执行终止状态**：`_terminated` 标志触发后，剩余步骤标记为 `not_executed`，报告状态显示为 `terminated`（执行终止），区别于 `failed`（执行失败）；
  ⑥ **修复 — store.update_step 幂等回写**：post-check 回写步骤失败状态时不再被幂等检查丢弃，支持更新已有步骤的状态/错误/耗时；
  ⑦ 涉及文件：`ui_headless_engine.py`、`ui_execution_store.py`、`ui_testing_report.html`。

- v1.31.5：headless 执行进度条实时更新 —
  ① **修复 — 进度条不更新**：根因是 worker 子进程执行完所有步骤后才输出结果，父进程 `process.wait()` 阻塞等待。执行期间 status API 返回空的 steps 数组，前端 `completed === 0` 一直显示"浏览器启动中"。修复方案：worker 每步完成时通过 `on_step_complete` 回调实时写 JSON 行到 stdout；父进程改为逐行循环读取，即时调用 `update_step()` 更新 store；前端修正标签逻辑，`running` 但无步骤时显示"执行中... 等待第一个步骤"；
  ② 涉及文件：`ui_headless_worker.py`（on_step_complete 回调）、`ui_execution_manager.py`（逐行读取 + `_parse_line` 方法）、`ui_testing_report.html`（标签逻辑）。

- v1.31.4：ruff 全量修复 26 个预存错误 —
  ① E722 裸 except ×5 → `except Exception`；
  ② F841 未使用变量 ×15 → 加 `_` 前缀；
  ③ F401 未使用导入 ×1 → `# noqa: F401`；
  ④ E402 导入不在顶部 ×2 → `# noqa: E402`；
  ⑤ E731 lambda 赋值 ×1 → 改为 `def` 函数；
  ⑥ ruff 零错误，2129 pytest 全绿。

- v1.31.3：每晚自动升级 — ruff 格式化。

- v1.31.2：new_tab 导航验证 + networkidle 回归 + 启动 loading 可见性修复 —
  ① **修复 — new_tab 导航验证**：new_tab 步骤不再仅凭 `actual_url` 有值就标记 passed，改为验证 `page.goto()` 成功 + URL 匹配目标。导航失败时标记为 failed 并终止测试（break），避免后续步骤在错误页面执行。增加 `_urls_match()` 静态方法比较 hostname + path；
  ② **修复 — new_tab 后 SPA 渲染等待**：new_tab 导航后恢复 `networkidle` 等待（仅 new_tab，初始导航不恢复），确保 Vue/React SPA 页面异步组件渲染完成后再执行后续步骤。3 次测试 2 次全部通过；
  ③ **修复 — 启动 loading 可见性**：前端在 status 为 running 但 completed === 0 时也显示"浏览器启动中……"，解决 starting 状态太短暂用户看不到的问题；
  ④ 2129 pytest 全绿，ruff 零错误。

- v1.31.1：浏览器启动 loading + 去除 networkidle + 启动时间剔除 —
  ① **新功能 — 浏览器启动 loading 界面**：headless 执行时增加"starting"状态，前端显示"浏览器启动中……"进度条。worker 子进程在浏览器就绪后写 `BROWSER_READY` 信号到 stdout，父进程逐行读取并更新状态为 running。涉及：`ui_headless_engine.py`（`on_browser_ready` 回调）、`ui_headless_worker.py`（`_write_line` 信号）、`ui_execution_manager.py`（逐行读 stdout）、`ui_execution_store.py`（`update_status` 方法）、`ui_testing_report.html` / `ui_testing_reports.html`（starting 状态 UI）；
  ② **性能优化 — 去除 networkidle 等待**：`page.goto()` 后不再等待 `networkidle`，节省约 5 秒启动时间；
  ③ **启动时间剔除**：`start_time` 移到 `page.goto()` 之后，浏览器启动时间不再计入 `total_duration_ms`，节省约 1.2 秒；
  ④ 2129 pytest 全绿，ruff 零错误。

- v1.31.0：步骤截图 + headless 子进程隔离 + 编码修复 —
  ① **新功能 — 步骤截图**：用例编辑器中每个步骤增加"截图"勾选项，勾选的步骤在 headless 执行时自动截图并在报告中显示。涉及文件：`templates/ui_testing_editor.html`（复选框列）、`templates/ui_testing_report.html`（截图链接显示）、`postman_api_tester/services/ui_headless_engine.py`（`_take_screenshot` 增加 suffix 参数，勾选步骤通过时也截图，保存为 `step_{i}.png`；失败截图保持 `step_{i}_fail.png`）、`postman_api_tester/handlers/ui_execution_routes.py`（截图 GET 端点兼容新旧文件名，报告 handler 透传 `screenshot` 字段）；
  ② **修复 — headless greenlet 线程错误**：Waitress 线程池与 Playwright sync API 的 greenlet 存在线程冲突，`browser.new_context()` 时抛出 `greenlet.error: cannot switch to a different thread`。改为 `subprocess.Popen` 在独立子进程中运行 Playwright，彻底隔离线程环境。新增 `postman_api_tester/services/ui_headless_worker.py`（子进程入口，通过 JSON 文件传递输入），重写 `ui_execution_manager.py` 的 `start_headless()` 方法（线程 → 子进程）；
  ③ **修复 — Windows 子进程中文编码**：worker 子进程用 `sys.stdout.write()` 输出 JSON 时，Windows 默认编码为 GBK，导致中文字符（如选择器文本 `登录`）被转码为 GBK 字节存储为乱码。改为 `sys.stdout.buffer.write(json_str.encode("utf-8"))` 强制 UTF-8 输出；
  ④ **性能优化 — headless 执行时间**：`delay_between_steps` 默认从 500ms 降至 200ms；同页面操作跳过步骤间延迟（仅页面导航后等待）；初始页面加载 `wait_until="load"` + `networkidle(timeout=5000)`；`_find_element` 增加 `is_visible()` 快速路径；执行时间从 ~26s 降至 ~16s；
  ⑤ **new_tab 跨系统跳转**：增加 popup 监听和 URL 捕获，支持基础平台 → 菜谱系统的跨域导航（`_on_popup` 回调 + `captured_new_tab_url` 状态传递）；
  ⑥ `execute()` 返回值新增 `_step_results` 字段，收集所有步骤结果供子进程回传；
  ⑦ 2129 pytest 全绿，ruff 零错误。
