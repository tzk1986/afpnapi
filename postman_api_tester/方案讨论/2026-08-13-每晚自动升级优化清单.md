# 每晚自动升级优化清单

**版本基线**：v1.33.34（2026-08-24）
**走查日期**：2026-08-13
**用途**：供每晚 20:30 自动升级任务参考

---

## 一、已完成优化（v1.33.0 ~ v1.33.34）

以下问题已在之前的版本中修复：

### 严重问题 ✅
- **C-1**: `_active_jobs` 并发竞争 — 已加锁保护
- **C-2**: `report_server.py` 业务逻辑泄漏 — 已迁移至 handlers
- **C-3**: `core/variable_context.py` 跨层引用 services — 已修复

### 高优先级 ✅
- **H-1**: SSRF 防护约束文档补充 — 已在开发阅读文档明确"UI 代理链路禁止套用 is_safe_url()"
- **H-2**: Cookie 明文写入日志 — 已脱敏处理
- **H-3**: handlers 直接文件 I/O — 已下沉到 services
- **H-4**: `_get_proxy_session_id()` 过长函数 — 已拆分
- **H-5**: `ui_testing_proxy()` 职责过重 — 已拆分

### 中优先级 ✅
- **M-1**: `normalize_url_and_params` 双实现 — v1.33.33 统一至 `utils/url_utils.py`
- **M-2**: `models.py` / `report_handler.py` 函数重复 — v1.33.34 统一到 `models.py`，删除 150+ 行重复代码
- **M-3**: 两个录制会话管理类功能重复 — 已合并
- **M-4**: `_check_proxy_host_allowed` 重复实现 — 已抽取公共函数
- **M-5**: handlers 中重复 try-except 模式 — 已引入 `@handle_api_errors` 装饰器
- **M-6**: `get_report_or_error` 返回类型不一致 — v1.33.32 改为异常模式，消除 13 处 isinstance 检查
- **M-9**: 回放引擎 NetworkComparator 在代理上下文中失效 — 已修复

### 低优先级 ✅
- **L-6**: Jinja2 `autoescape=False` — 已改为 `True`

---

## 二、待优化项（按优先级排序）

### 🟡 中优先级（M 系列）

#### M-1: 合并 `normalize_url_and_params` 双实现 ✅ **v1.33.33 已完成**

**文件**: `runtime_utils.py`、`utils/url_utils.py`

**问题**: 两个实现功能相同但语义有差异。

**实施记录**: v1.33.33
- 保留 `utils/url_utils.py` 版本作为权威实现（支持 list 格式）
- 修复 `url_utils` 中空路径被错误转换为 "/" 的问题
- 删除 `runtime_utils.py` 中的重复实现，改为从 `url_utils` 导入
- 更新 `auth.py`、`http_handler.py` 的 import 路径
- 更新 `test_runtime_utils.py` 移除重复测试类
- 全量测试 2210 通过，mypy 零错误

**影响评估**: 安全 ✅

---

#### M-2: 消除 `models.py` / `report_handler.py` 函数重复 ✅ **v1.33.34 已完成**

**文件**: `models.py`、`handlers/report_handler.py`

**问题**: `filter_report_results`、`paginate_items`、`compare_report_data`、`_map_results`、`_to_rate` 在两个文件中重复实现。

**实施记录**: v1.33.34
- 将 `repeat_index`、`repeat_total`、`repeat_group` 三个字段合并到 `models.py` 版本
- `report_handler.py` 仅保留 `normalize_status_filter`（路由层状态筛选归一化专用）
- 删除 150+ 行重复代码
- 更新测试：`test_report_handler.py` 仅测试 `normalize_status_filter`，`test_models.py` 新增 `filter_report_results` 完整测试（含 repeat 字段验证）
- 全量测试 2195 通过，mypy 零错误

**影响评估**: 安全 ✅

---

#### M-6: 统一 `get_report_or_error` 为异常模式 ✅ **v1.33.32 已完成**

**文件**: `handlers/base_handler.py`

**问题**: 返回类型为 `Any`（`Union[Dict[str, Any], ResponseReturnValue]`），每个调用方都做 `isinstance(report, tuple)` 类型检查（重复 13 处）。

**整改方案**:
- 新增 `ReportNotFoundError` 异常类，携带 `message` 和 `error_code`
- 新增 `register_error_handlers()` 辅助函数，为 Flask app 注册全局错误处理器
- `get_report_or_error` 改为抛出异常，不再返回 tuple
- Flask 错误处理器捕获异常并返回统一 JSON 404 响应
- 移除 13 处 `isinstance(report, tuple)` 检查和 `cast()` 调用

**实施记录**: v1.33.32
- 新增 `ReportNotFoundError` 异常类和 `register_error_handlers()` 函数
- 更新 `export_routes.py`、`collection_routes.py`、`report_meta_routes.py`、`report_result_routes.py`、`retry_routes.py` 共 5 个路由文件
- 更新 4 个测试 fixture 注册错误处理器，更新 3 个测试用例捕获异常
- 全量测试 2218 通过，mypy 零错误

**影响评估**: 安全 ✅

---

#### M-7: `@require_global_variables_enabled` 装饰器 ✅ **v1.33.6 已完成**

**文件**: `handlers/global_variables_routes.py` 多处

**问题**: 7 处重复 `_check_enabled()` 检查。

**整改建议**:
- 引入 `@require_global_variables_enabled` 装饰器
- 注意 `api_env_list_get` 有降级行为（返回默认环境列表而非 403），不应被装饰器覆盖

**影响评估**: 安全 ✅

**实施记录**: v1.33.6 已完成
- 新增 `@require_global_variables_enabled` 装饰器
- 6 个函数使用装饰器替代重复检查
- `api_env_list_get()` 保持降级行为，不使用装饰器
- 全量测试 2154 通过

---

#### M-8: 录制器事件监听清理 ⏳

**文件**: `services/ui_recorder_inject.py`

**问题**:
- `setInterval(_processEventQueue, 100)` 录制停止后仍在运行
- `window` + `document` + `document.body` 重复注册相同事件类型的监听器（12+ 个）
- 早期脚本和主脚本双重事件捕获路径导致步骤重复录制

**整改建议**:
- 录制停止时 `clearInterval()`
- 移除 `document` 和 `document.body` 上的重复监听器
- 统一事件捕获路径（仅使用早期脚本的队列机制）

**影响评估**: JS 层改动，暂缓

---

#### M-10: 录制会话仅内存存储，服务重启丢失

**文件**: `handlers/ui_testing_routes.py` 第 30-83 行

**问题**: 录制会话数据仅保存在内存中，服务重启后丢失。

**整改建议**:
- 将录制会话数据迁移到 `services/ui_recording_store.py`
- 添加定期 JSON 持久化（如每 30 秒写入一次）
- 服务启动时从磁盘恢复未完成会话

**影响评估**: 安全 ✅

---

#### M-11: UI 执行结果无定期自动清理 ✅ **已被定时任务覆盖**

**问题**: `UiExecutionStore.cleanup_expired()` 仅通过 API 手动触发，无定时任务。

**整改状态**: 已通过每晚 20:30 自动升级任务解决（cron: `30 20 * * *`）

---

### 🟢 低优先级（L 系列）

#### L-1: `BaseHandler.json_response()` 未被统一使用

**问题**: 10+ 个 handler 文件直接使用 `jsonify()` 而非 `BaseHandler.json_response()`，导致响应格式不一致。

**整改建议**:
- 逐步统一为 `BaseHandler.json_response()`
- 确保所有 JSON 响应都有 `{code, message, data, timestamp}` 结构

**影响评估**: 安全 ✅ — 影响面大，按需迭代

---

#### L-2: `get_report_or_error` 返回类型为 `Any`

**文件**: `handlers/base_handler.py` 第 195 行

**整改建议**: 标注为 `Union[Dict[str, Any], ResponseReturnValue]` 或改为抛出异常（同 M-6）

---

#### L-3: 硬编码魔法数字 ✅ **v1.33.22 已完成**

| 位置 | 值 | 建议 | 状态 |
|------|-----|------|------|
| `ui_testing_routes.py` 第 449 行 | `5` | 提取为 `MAX_PROXY_UNWRAP_DEPTH` | ✅ 已提取 |
| `ui_testing_routes.py` 第 654 行 | `3600` | 提取为 `PROXY_COOKIE_MAX_AGE` | ✅ 已提取 |
| `ui_execution_routes.py` 第 727 行 | `10` | 提取为 `WEBHOOK_TIMEOUT` | ✅ 已提取 |
| `server_routes.py` 第 450/476 行 | `5000` | 提取为 `DEFAULT_PORT` | ✅ 已重构至 config |
| `ui_testing_routes.py` 代理检测 | `5000` | 改用 `REPORT_SERVER_PORT` | ✅ v1.33.22 |

**整改建议**: 提取为命名常量

**实施记录**: v1.33.22 完成最后一处端口硬编码消除

---

#### L-4: 命名约定不一致

**问题**: 
- `get`/`find`/`load`/`read` 混用表示查询操作
- `api_collection_parse`/`api_collection_save` 使用非标准动词
- `api_report_result_judgement` 使用名词而非动词

**整改建议**:
- 制定命名约定文档
- 优先统一 `get_*`（单个）/ `list_*`（列表）/ `load_*`（持久化加载）
- 迭代修正，不一次性大规模重构

---

#### L-5: `Dict[str, Any]` 过度使用

**问题**: 核心数据结构（报告记录、执行结果、用例数据）均使用 `Dict[str, Any]`，无法利用 IDE 补全和静态检查。

**整改建议**:
- 为关键数据结构引入 `TypedDict`：
  - `ReportResultDict`（报告结果条目）
  - `ExecutionStepDict`（执行步骤）
  - `UITestCaseDict`（UI 用例）

---

#### L-7: 超长文件需拆分

| 文件 | 行数 | 建议 |
|------|------|------|
| `services/ui_recorder_inject.py` | 2134 | JS 模板字符串为主，可接受 |
| `services/ui_proxy_service.py` | 1868 | 可拆分为 `proxy_service.py` + `proxy_html_rewriter.py` |
| `services/ui_headless_engine.py` | 1256 | 可拆分为 `headless_engine.py` + `headless_newtab.py` |
| `handlers/ui_testing_routes.py` | 1176 | 拆为 `ui_testing_routes.py` + `ui_proxy_routes.py` + `ui_recording_routes.py` |

**整改建议**: 按模块职责拆分，保持单一职责原则

---

#### L-8: 登录检测改为元数据判断 ✅ **v1.33.22 已完成**

**文件**: `ui_headless_engine.py`

**问题**: 依赖中文字符判断登录按钮，国际化场景下会失效。

**整改建议**: 改为基于步骤元数据（如 `step.is_login_step`）或选择器类型判断

**实施记录**: v1.33.22 已完成
- 新增 `_is_login_step()` 辅助函数
- 支持 `is_login_step` 元数据标记（最高优先级）
- 中文检测保留"登"+"录"同时出现（避免"登山""记录"误匹配）
- 新增英文关键词支持：`login`/`sign in`/`signin`/`log in`
- 同时检查 `element_info.text`（录制时捕获的元素文本）
- 新增 12 个单元测试覆盖

---

## 三、每晚自动升级执行策略

### 3.1 优先级选择

每次自动升级时，按以下顺序选择优化项：

1. **中优先级（M 系列）**：优先处理影响代码质量和可维护性的问题
2. **低优先级（L 系列）**：按需迭代，不影响核心功能

### 3.2 实施原则

1. **每项改动独立提交**：便于回退和定位问题
2. **每项改动后运行全量测试**：确保无回归
   - `pytest` 全通过
   - `mypy postman_api_tester` 零错误
3. **优先改动测试覆盖充分的模块**
4. **有风险项（M-1/M-2）先对齐语义差异**：再统一实现

### 3.3 版本号递增

- **PATCH 版本**：修复 bug、代码重构、性能优化（如 M-4、M-5）
- **MINOR 版本**：新增功能、较大改进（如 M-10、M-11）
- **MAJOR 版本**：大改版、破坏性变更（当前无此类优化）

### 3.4 完成标记

每完成一个优化项后，在本文件中标记：
- ✅ 已完成
- ⏳ 暂缓（说明原因）
- ❌ 取消（说明原因）

---

## 四、风险与注意事项

### 4.1 高风险项

- **M-1/M-2**：涉及语义差异和字段差异，需先充分测试对齐
- **M-8**：JS 层改动，需回归测试录制功能
- **L-7**：文件拆分可能影响 import 路径，需全量检查

### 4.2 低风险项

- **M-6/M-7**：装饰器模式，代码更清晰，风险低
- **M-10**：增加持久化，不影响现有逻辑
- **L-3**：提取常量，纯重构
- **L-4**：命名约定，可逐步迭代

### 4.3 不改动项

- **M-11**：已通过定时任务解决
- **H-1**：UI 代理端点禁止添加 `is_safe_url()` 检查（历史 bug 教训）

---

## 五、参考文档

- `postman_api_tester/开发阅读文档.md`：完整开发约束
- `postman_api_tester/方案讨论/2026-08-11-代码走查优化方案.md`：详细问题分析
- `postman_api_tester/方案讨论/2026-08-11-网络请求捕获链路补全方案.md`：M-9 修复方案

---

**维护提示**：每次自动升级后更新本文件的"已完成"状态和版本号基线。
