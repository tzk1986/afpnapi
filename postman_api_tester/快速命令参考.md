# Postman API 测试工具 - 快速命令参考（v1.8.4）

发布日期：2026-06-16
文档定位：命令速查，适合熟悉流程后快速执行、快速排错、快速确认参数优先级。

关联文档：

- `postman_api_tester/README.md`：第一次接手项目时先看整体目录和上手顺序。
- `postman_api_tester/操作手册.md`：需要完整解释、页面说明、迁移验证建议时继续查看。
- `postman_api_tester/最终交付清单.md`：需要确认交付范围、验收要求时查看。
- `postman_api_tester/开发阅读文档.md`：需要修改默认行为或核对约束时查看。
- `postman_api_tester/日志事件字典.md`：需要按 event 维度检索日志时查看。
- `postman_api_tester/日志字段规范.md`：需要按统一字段配置日志平台或告警规则时查看。

文档统一目录：`postman_api_tester/`

## 1. 先看哪几个文件

新人最常会用到：

- `postman_api_tester/config.py`：改地址、Token、报告目录、超时
- `postman_api_tester/postman_api_tester.py`：命令行执行入口
- `postman_api_tester/run_test_and_open.py`：交互式快速执行
- `postman_api_tester/__main__.py`：报告服务启动入口（`python -m postman_api_tester`）
- `postman_api_tester/README.md`：新人入口文档
- `postman_api_tester/操作手册.md`：完整说明

目录与调用路径速记：

- 启动入口：`python -m postman_api_tester` -> `postman_api_tester/__main__.py` -> `postman_api_tester/report_server.py`
- 路由分层：`report_server.py` 路由 -> `handlers/*` -> `services/*` -> `report_repository.py` / `report_meta_repository.py` / `report_job_store.py`
- 通用支撑：`utils/*` 提供 URL 合并、请求构建、响应解析、脱敏、缓存
- 执行落点：`handlers/job_routes.py` 编排任务入队，最终调用 `postman_api_tester.py::run_postman_tests()`

## 2. 环境准备

```bash
cd d:\tangzk\py\seldom-api-testing
pip install -r requirements.txt
```

## 3. 最常用命令

```bash
# 方式1：模块方式（推荐）
python -m postman_api_tester.postman_api_tester "支付（UPS）.postman.json"

# 方式1.1：第一次打通环境建议先用示例文件
python -m postman_api_tester.postman_api_tester "sample_api_collection.json"

# 指定 base_url
python -m postman_api_tester.postman_api_tester "支付（UPS）.postman.json" "http://10.50.11.130:11000"

# 指定输出目录
python -m postman_api_tester.postman_api_tester "支付（UPS）.postman.json" "http://10.50.11.130:11000" "reports"

# 指定 token（优先级最高）
python -m postman_api_tester.postman_api_tester "支付（UPS）.postman.json" "http://10.50.11.130:11000" "reports" "your_token"

# 指定分页大小（第5个参数）
python -m postman_api_tester.postman_api_tester "支付（UPS）.postman.json" "http://10.50.11.130:11000" "reports" "your_token" 50
```

补充：

- 如不传 `output_dir`，将按 `postman_api_tester/config.py` 中 `REPORT_OUTPUT_DIR`（为空则默认 `reports/`）
- 如不传 `token`，按当前优先级会尝试 `POSTMAN_TOKEN`、`config.py` 中的 `TOKEN`，最后再走自动登录

## 4. 配置文件最常改项

编辑文件：`postman_api_tester/config.py`

最常改的配置：

```python
BASE_URL = "http://10.50.11.130:11000"
TOKEN = ""
REPORT_OUTPUT_DIR = r"D:\api-test-reports"
REQUEST_CONNECT_TIMEOUT = 10
REQUEST_READ_TIMEOUT = 30
ENABLE_SELECTIVE_RUN = True
ENABLE_ADHOC_RUN = True
ADHOC_MAX_ITEMS = 200
ADHOC_DEFAULT_COLLECTION_NAME = "报告中心临时测试"
REPORT_EXPORT_DEFAULT_SCOPE = "full"
REPORT_EXPORT_CHANNEL_MODE = "auto"     # auto | legacy | stream
REPORT_EXPORT_STREAM_THRESHOLD = 800      # auto 模式下总接口数阈值
# v1.2.0+ 配置项
ENABLE_RESPONSE_TIME = True            # 记录并展示每接口响应时间（默认 True）
ENABLE_RETRY_FAILURES = True           # 报告页启用"一键重试失败用例"（默认 True）
ENABLE_JUNIT_EXPORT = True             # 启用 JUnit XML 导出链接（默认 True）
ENABLE_REPORT_LIST_FILTER = True       # 首页报告列表筛选面板（默认 True）
ENABLE_REPORT_ANALYTICS = True         # 启用分析洞察页签与 analytics 接口（默认 True）
ENABLE_REPORT_ANALYTICS_CHARTS = True  # 启用分析洞察图表可视化（默认 True，需父开关 ENABLE_REPORT_ANALYTICS 同时开启）
REPORT_ANALYTICS_TOP_N_DEFAULT = 10    # analytics TopN 默认值
REPORT_ANALYTICS_TOP_N_MAX = 100       # analytics TopN 上限
REPORT_ANALYTICS_TREND_LIMIT_DEFAULT = 20  # 趋势默认报告数
REPORT_ANALYTICS_TREND_LIMIT_MAX = 100     # 趋势上限
REPORT_ANALYTICS_ENABLE_SAMPLES = False    # 默认不返回错误样本
REPORT_ANALYTICS_HISTOGRAM_BUCKETS = "0,50,100,200,500,1000,3000,5000"
QUALITY_SCORE_FAILED_PENALTY = 10      # FAILED 扣分
QUALITY_SCORE_ERROR_PENALTY = 15       # ERROR 扣分
QUALITY_SCORE_SLOW_PENALTY = 5         # 慢请求扣分
QUALITY_SCORE_ASSERTION_MISSING_PENALTY = 2  # 缺少断言扣分
ENABLE_ASSERTIONS = False              # JSONPath 断言校验（默认 False，需显式开启）
ASSERTIONS_ENGINE = "jsonpath"         # 断言引擎类型（默认 jsonpath）
ENVIRONMENTS = {}                      # 多环境字典，键=环境名，值={base_url, token}
DEFAULT_ENV_NAME = ""                  # 默认激活的环境名
ENABLE_CHECKPOINT_RECOVERY = False     # 断点恢复（默认关闭，保持旧行为）
CHECKPOINT_FLUSH_EVERY_N = 1           # checkpoint 写入频率
CHECKPOINT_DIR = ""                    # checkpoint 目录（空则 reports/checkpoints）
ENABLE_ASSERTION_STRICT_MODE = False   # 断言引擎异常严格失败模式
# v1.3.3+ 可配置结果自动判定
ENABLE_ERR_CODE_JUDGMENT = False       # 启用 errCode 参与结果判定（默认关闭，保持旧行为）
SUCCESS_ERR_CODES = "0"                # 视为成功的 errCode 值列表（逗号分隔）
ENABLE_MESSAGE_JUDGMENT = True         # 启用 message 参与结果判定（默认开启）
SUCCESS_MESSAGES = "success"           # 视为成功的 message 值列表（逗号分隔）
# v1.4.0+ 数据驱动与变量提取
ENABLE_DATA_DRIVEN = False             # 启用数据驱动测试（默认关闭）
DATA_FILE_MAX_ROWS = 10000             # 数据文件最大行数
DATA_FILE_MAX_SIZE = 10485760          # 数据文件最大字节数（默认 10MB）
ENABLE_VARIABLE_EXTRACTION = False     # 启用请求串联与变量提取（默认关闭）
LOG_LEVEL = "INFO"                     # 日志级别
LOG_FORMAT = "structured"              # 日志格式
LOG_SAMPLE_RATE = 0.1                  # 高频日志采样率
LOG_ALERT_ERROR_WINDOW_SECONDS = 300   # ERROR 速率统计窗口（秒）
LOG_ALERT_ERROR_RATE_THRESHOLD_PER_MIN = 10  # ERROR 告警阈值（每分钟）
```

推荐顺序：

1. 先改 `BASE_URL`
2. 再决定 `TOKEN` 填不填
3. 再确认 `REPORT_OUTPUT_DIR`
4. 超时不够再改 `REQUEST_CONNECT_TIMEOUT` / `REQUEST_READ_TIMEOUT`

也可用环境变量临时覆盖：

```powershell
$env:POSTMAN_BASE_URL = "http://10.50.11.130:11000"
$env:POSTMAN_TOKEN = "your_token"
python -m postman_api_tester.postman_api_tester "sample_api_collection.json"
```

## 5. Python 调用方式

```python
from postman_api_tester.postman_api_tester import run_postman_tests
report = run_postman_tests(
    postman_file="支付（UPS）.postman.json",
    base_url="http://10.50.11.130:11000",
    output_dir="reports",
    token=None,
    results_per_page=30,
    # v1.4.0+ 可选参数
    data_file="test_data/users.csv",                    # 数据驱动文件（CSV/JSON）
    initial_variables={"auth_token": "xxx"},            # 预置变量（请求串联）
)
print(report.generate_summary())
```

## 6. 类型检查（3.2）

```bash
# 分阶段类型检查（当前已收口模块）
python -m mypy --config-file pyproject.toml --explicit-package-bases
```

说明：当前分阶段纳管文件由 `pyproject.toml` 的 `tool.mypy.files` 统一维护。

## 7. 报告查看

```bash
# 生成文件在 reports/ 目录
# 例如：reports/postman_report_20260416_190000.html

# 如需历史报告、差异对比、Token测试、重新请求、局域网访问
python -m postman_api_tester
# 浏览器打开 http://localhost:5000

# 健康检查（含日志告警快照）
curl http://127.0.0.1:5000/health
```

报告中心常用入口：

- 首页：历史报告、删除报告、差异对比
- 上传区：支持先解析接口，再按”全量/仅已选接口”执行；支持可配置结果判定条件（errCode 成功值、message 成功值，填写即启用覆盖全局配置）
- ad-hoc 独立页入口：支持从首页跳转到"新增接口测试"页面，再录入接口并执行生成报告（无需先准备完整 Collection 文件）；支持多环境下拉选择与 JSONPath 断言配置（需 `ENABLE_ASSERTIONS=true`）
- 响应时间展示：数据视图结果列表新增"耗时(ms)"列；底部汇总区显示 avg/max/p95 响应时间（受 `ENABLE_RESPONSE_TIME` 控制）
- 一键重试失败用例：报告页工具栏"重试失败用例"按钮，后端调用 `POST /api/retry-failures` 重跑 FAILED/ERROR 接口（受 `ENABLE_RETRY_FAILURES` 控制）
- JUnit XML 导出：报告页工具栏"导出 JUnit XML"链接，下载 JUnit 格式报告，用于 CI/CD 集成（受 `ENABLE_JUNIT_EXPORT` 控制）
- 首页列表筛选：首页报告列表支持通过状态/最低通过率/日期范围快速筛选，一键重置（受 `ENABLE_REPORT_LIST_FILTER` 控制）
- 分析洞察：单报告页 analytics 页签展示分布、性能、错误诊断、质量评分、覆盖率和趋势（受 `ENABLE_REPORT_ANALYTICS` 控制）
- 报告对比：analytics 页签内可选择左右报告，对比成功率、平均响应、失败数、错误数和质量分变化
- 数据视图：查看每个接口的请求/响应详情
- 人工用例编辑器：三段式流程（发送请求 -> 回填响应 -> 选择 PASSED/FAILED 并保存）
- 编辑界面切换与 Body 类型选择：mc/er 双编辑器切换隔离，支持 none/raw/urlencoded/formdata/graphql/binary，切换时互不污染
- 人工用例编辑回显：编辑时自动回显历史请求信息（Headers/Params/Body），可改参后重发
- 人工用例详情响应体：与报告详情同款展示逻辑（预览/展开）
- 页面模板更新时间标记：报告页顶部可快速确认当前模板版本是否已更新
- 结果行内排除：在列表行直接切换“排除/取消排除”
- 原始 HTML：打开原始静态报告
- 元数据：查看结构化摘要和结果索引
- 导出最新 JSON：支持 `full` 和 `report_only`
- 详情人工判定：结果详情支持“标记成功/标记失败/恢复自动结果”，可直接修正状态并刷新 summary
- 实际URL去重：详情“实际请求 URL”与重试链路按 URL+params 去重合并，避免 query 重复

导出范围速记（full vs report_only）：

- `full`：导出源 Collection 的全部接口（再叠加人工用例并减去已排除项）。
- `report_only`：仅导出“本次报告执行到的接口”（再叠加人工用例并减去已排除项）。
- 两者何时相同：当本次报告已覆盖源 Collection 全部接口时，导出内容会一致（页面会提示“范围等价”）。
- 选型建议：需要完整回归包选 `full`；需要最小复现包选 `report_only`。

导出通道速记（auto vs legacy vs stream）：

- `auto`：默认模式。总接口数 >= `REPORT_EXPORT_STREAM_THRESHOLD` 时优先流式，失败自动回退旧接口。
- `legacy`：强制旧接口 `/api/export-collection`。
- `stream`：强制流式接口 `/api/export-collection-stream`。

ad-hoc 快速约束：

- 后端入口：`/api/run-ad-hoc-tests`
- 输入主体：JSON `cases` 数组，至少 1 条，数量上限受 `ADHOC_MAX_ITEMS` 控制
- URL 规则：支持绝对 `http/https`、`{{baseUrl}}/...` / `{{base_url}}/...`、或配合 `base_url` 的相对路径
- 执行链路：先写入 `uploaded_collections/<job_id>.json`，再复用既有任务状态接口与报告生成链路

人工用例发送补充：

- 发送请求走后端代理接口，仅允许 `http/https`。
- 编辑重试接口 `/re-request-api` 同步限制 `http/https`。
- 请求超时遵循 `postman_api_tester/config.py` 中 `REQUEST_CONNECT_TIMEOUT` 和 `REQUEST_READ_TIMEOUT`。

可复用冒烟脚本：

- 启动服务后执行：`python test_data/smoke_report_server_20260427.py`
- 执行链路一键冒烟（推荐）：`python -m test_data.smoke_execution_chain_bundle_20260511`
- 也可单独执行：
    - `python -m test_data.smoke_auth_token_fallback_20260511`
    - `python -m test_data.smoke_auth_token_missing_field_fallback_20260511`
    - `python -m test_data.smoke_assertion_strict_mode_20260511`
    - `python -m test_data.smoke_executor_result_shape_20260511`
    - `python -m test_data.smoke_sensitive_headers_20260511`
    - `python -m test_data.smoke_collection_query_service_20260511`
    - `python -m test_data.smoke_report_analytics_20260514`
    - `python -m test_data.smoke_report_analytics_compare_20260514`

analytics 常用验证：

```powershell
# 单报告 analytics
curl "http://127.0.0.1:5000/api/report-analytics/%E6%96%B0%E5%89%8D%E5%8E%85%E6%9F%A5%E8%AF%A2.html?top_n=10&trend_limit=20&include_samples=false"

# 左右报告对比
curl "http://127.0.0.1:5000/api/report-analytics-compare?left=%E6%96%B0%E5%89%8D%E5%8E%85%E6%9F%A5%E8%AF%A2.html&right=%E5%89%8D%E5%8E%85%E6%9F%A5%E8%AF%A2.html"

# 灰度关闭 analytics
$env:ENABLE_REPORT_ANALYTICS = "false"
python -m postman_api_tester
```

如果报告目录不是默认 `reports/`，可选两种方式：

```powershell
# 方式1：环境变量指定
$env:POSTMAN_REPORTS_DIR = "D:\api-test-reports"
python -m postman_api_tester

# 方式2：在 config.py 中设置 REPORT_OUTPUT_DIR
```

补充说明：

- 首页可查看历史报告列表
- 点击“数据视图”可进入单份报告的结果浏览页
- 单报告页支持按名称/路径搜索、状态筛选、分页和详情查看
- 单报告页失败/异常结果可看到数据库迁移反馈提示
- 可选择两份报告进行差异对比
- 同局域网可通过 `http://本机局域网IP:5000` 访问

## 6.1 Windows 防火墙一键放通 5000 端口

```powershell
cd d:\tangzk\py\seldom-api-testing
powershell -ExecutionPolicy Bypass -File .\allow_report_server_firewall.ps1
```

- 首次执行会弹出管理员授权
- 放通后即可让同局域网通过 `http://本机局域网IP:5000` 访问

## 7. 报告目录优先级

报告中心读取目录优先级：

1. `POSTMAN_REPORTS_DIR`
2. `REPORTS_DIR`
3. `postman_api_tester/config.py` 中的 `REPORT_OUTPUT_DIR`
4. 默认 `项目根目录/reports`

## 8. Token / 地址优先级

1. 命令行/代码传入 token 参数
2. 环境变量 `POSTMAN_TOKEN`
3. `postman_api_tester/config.py` 的 `TOKEN`
4. 自动识别 login 接口并获取

base_url 优先级：

1. 命令行/代码传入 base_url
2. 环境变量 `POSTMAN_BASE_URL`
3. `postman_api_tester/config.py` 的 `BASE_URL`
4. 从 Postman 集合解析

## 9. 常见问题快速处理

- 报错 ModuleNotFoundError: 先执行 `pip install -r requirements.txt`
- 报错 FileNotFoundError: 检查 JSON 路径是否相对当前目录正确
- 报告打不开或功能不完整: 使用 `python -m postman_api_tester` 启动本地服务
- 失败详情中看到“数据库反馈”: 优先按提示判断是否为连接、认证、SQL兼容、schema、字符集或类型兼容问题
- 导出后二次导入出现“预览 2 条但执行 1 条”: 当前版本已修复；执行层会在 `request.url.path` 缺失时回退解析 `request.url.raw`
- 导出后二次导入后人工用例请求体为空：当前版本已修复；导出文件现会保留人工用例完整请求参数（headers/params/body）
- “全量导出 / 导出本次报告接口”看起来一样：当前版本已补充提示；当源集合与本次报告接口集合一致时，二者导出内容本就相同，导出弹窗会说明该情况
- 导入执行接口点击“排除”不生效：当前版本已修复；排除键已统一规范并兼容旧格式，`include_excluded=false` 会正确隐藏已排除项
- 人工用例新增后仍在列表最后：当前版本已修复；旧人工用例缺失 `created_at` 时不再临时补当前时间，排序不会漂移
- 数据视图搜索不作用于人工用例列表：当前版本已修复；搜索条件已同步过滤人工用例列表
- 控制台中文乱码: Windows 建议先执行 `$env:PYTHONIOENCODING="utf-8"`

## 10. 新人首次使用最短路径

```powershell
cd d:\tangzk\py\seldom-api-testing
pip install -r requirements.txt
# 先修改 postman_api_tester/config.py 里的 BASE_URL
python -m postman_api_tester.postman_api_tester "sample_api_collection.json"
python -m postman_api_tester
```

然后浏览器打开：`http://127.0.0.1:5000`

## 11. 发布信息

- 当前版本：v1.8.4
- 发布时间：2026-06-16
- 定位：功能增强版（接口管理增强）

> **约束记录维护提示**：`postman_api_tester/开发阅读文档.md` 中的「约束更新记录」必须按记录时间升序维护（同日按时分秒排序）；新增记录前需先走查本文件及四份关联文档，发现乱序、拼接、重复时必须先恢复排序与结构，再追加新记录。详见 `开发阅读文档.md` 约束 44。
